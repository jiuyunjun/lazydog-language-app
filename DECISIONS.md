---
doc: "DECISIONS.md"
tier: "L5 过程记录"
status: "生效"
version: "1.2"
updated: "2026-09-06"
authority: "已确认的决定（D-0xx），含对专项设计的有意偏离"
index: "DOCS.md"
maintenance: "改本文须同步 DOCS.md 的版本表，校验命令 python tools/check_docs.py"
---

# 决策记录

这里记录已经确认、会影响后续实现的决定。新决定追加记录；如果推翻旧决定，应保留旧记录并注明替代关系。

## D-001：产品形态

- 状态：已确认
- 决定：私人使用、中文母语者学习英语的 Android App。
- 原因：保持目标和首版范围清晰。

## D-002：主学习模式

- 状态：已确认
- 决定：同时提供 AI 今日任务和自由学习，但以今日任务为主。
- 原因：减少规划成本，同时保留用户主动性。

## D-003：首版深度优先

- 状态：已确认
- 决定：接受功能朴素，优先跑通单词、语法、阅读、朗读、测试和复习的完整链路。
- 原因：先验证闭环价值，避免多个孤立功能。

## D-004：初始水平

- 状态：已确认
- 决定：不要求用户自报等级，通过自适应测试建立初始画像，并在使用中持续修正。

## D-005：记忆与知识库

- 状态：已确认
- 决定：维护已学单词和语法知识库，按照遗忘规律安排复习；AI 不直接决定掌握状态。

## D-006：渐进式阅读

- 状态：已确认
- 决定：阅读以已掌握内容为主，混入到期复习内容和少量新知识。初始比例为产品假设，需通过实际使用调整。

## D-007：Search API

- 状态：已确认
- 决定：MVP 不接入 Search API，但阅读来源接口应允许以后加入。

## D-008：技术平台

- 状态：已确认
- 决定：Android 原生 Kotlin；优先 Jetpack Compose 和 Material 3 默认 UI；无自建服务端。

## D-009：密钥处理

- 状态：已确认
- 决定：接受私人 App 在客户端保存在线服务密钥；通过设置页保存，不将真实密钥硬编码或提交到 Git。

## D-010：每日默认量

- 状态：暂定，待实测
- 决定：10～15 分钟、最多 5 个新单词、最多 1 个新语法点、1 篇短文。复习积压时优先复习，不强制加入新内容。

## D-011：工程协作

- 状态：已确认
- 决定：全部文本文件使用 UTF-8；每个完成任务形成独立 Git 提交；ChatGPT/Claude 使用标准 handoff。

## D-012：本地写死服务配置（替代 D-009 的“设置页保存”部分）

- 状态：已确认（2026-08-04，用户决定）
- 决定：私人使用阶段，AI 与 Azure Speech 配置写死在 `core/config/LocalEnv.kt`，该文件被 `.gitignore` 忽略，仓库只保留 `LocalEnv.kt.example` 模板。onboarding 不再要求用户填写服务配置；DataStore 中存过的值优先于 LocalEnv，后续开放设置页手动配置时无需改读取逻辑。
- 原因：单人使用，降低每次安装的配置成本。“不将密钥提交到 Git”的底线不变。

## D-013：朗读（M5）提前到 M1 之后实现

- 状态：已确认（2026-08-04，用户决定）
- 决定：在 M2（能力测试）和 M3（AI 内容）之前先接入 Azure Speech 朗读：TTS 示范、录音发音评估和反馈。朗读结果反写复习计划仍留在 M6。
- 原因：Speech 密钥已配好且可独立验证；朗读闭环不依赖 AI provider。

## D-014：数据持久化用 SAF 选定文件夹 + 自动导出，不用 MANAGE_EXTERNAL_STORAGE

- 状态：已确认（2026-08-05，用户决定）
- 决定：不把 Room 数据库直接放进公共存储，也不申请"所有文件访问权限"。改为让用户在设置里通过标准的 SAF 文件夹选择器（`ACTION_OPEN_DOCUMENT_TREE`）授权一个外部文件夹，App 每天自动把知识库和学习偏好导出成一份 JSON 写进去；首次启动的欢迎页也能选同一个文件夹来恢复。
- 原因：MANAGE_EXTERNAL_STORAGE 需要用户去系统设置手动开一个特殊权限开关，且以后如果上架 Google Play 这个权限会被卡审核；SAF 不需要敏感权限，兼容性更好。代价是 App 卸载重装后，Android 的 URI 授权会失效（跟着应用签名+包名走），文件本身还在文件夹里，但用户需要在欢迎页重新选一次同一个文件夹才能恢复，不是完全零操作。
- 被否决方案：把 Room 数据库文件直接放在 `Environment.getExternalStorageDirectory()` 下的固定路径，靠 MANAGE_EXTERNAL_STORAGE 全权限读写——零操作但权限过重，个人分发虽然无所谓，但不符合"够用就好、不为将来可能的上架预留"的原则。

## D-015：能力测试和新词生成对齐《CEFR 英语能力评测与个性化学习系统设计.md》

- 状态：已确认（2026-08-05，用户决定）
- 决定：能力测试引擎换成该文档的规则——连续能力值加入 Pre-A1（0.0）锚点；客观题梯度覆盖约束从 4 类扩到 5 类（新增"纠错或短答"，允许部分正确，本地按编辑距离相似度打分，不用 AI 判分）；答对但用时异常（疑似猜测/明显超时）只算弱证据、涨分打对折。新词生成从"AI 随便挑词"改成按词形+词性+具体词义生成，每个词附 1~2 个真实高价值搭配，不再是孤立单词表。
- 原因：用户直接要求按新文档对齐。
- 明确没做的（文档 §8 自己也认为不受控生成会牺牲可靠性，个人开发阶段先不追）：审核过的分级词库 + IRT/Rasch 统计校准（题目和词义的 CEFR 等级目前仍是 AI 实时判断，不是"经过标定"）；词义级的 6 维掌握度模型（识别/语境/回忆/使用/搭配/发音分别打分）——现在仍是单一的 FSRS 式调度状态；候选词的完整优先级公式（频率×相关性×等级适配×迁移价值）——现在只用"已学过的词排除"做缺口代理，没有真实频率/语域数据支撑更精细的排序。这些留给以后有真实使用数据时再实现。

## D-016：情景演练难度与今日任务

- 状态：已确认（MVP 实现口径）
- 决定：用户只使用“换个更难的对手”整体切档，不直接调整五个独立旋钮；内部继续分别维护信息量、合作度、追问强度、礼貌拒绝和误解。情景演练是学习页第五个入口，不计入今日必做目标时长。
- 原因：保持开演前页面简单，同时不牺牲生成约束的可解释性。

## D-017：情景会话快照与全局英文手势

- 状态：已确认（2026-08-05，用户要求）
- 决定：情景演练用 Room 保存 JSON 会话快照，进入统一“最近材料”并支持中断恢复。主要英文学习内容统一使用快速双击查词、快速三击讲句；讲解复用阅读功能，词汇可进入统一生词本。
- 原因：情景演练属于可反复使用的学习材料，不能因退出而丢失；统一手势可避免每个页面重复设计一套查词入口。

## D-018：完整句子归入“表达”而不是“单词”

- 状态：已确认（2026-08-05，用户反馈）
- 决定：知识库显示为单词、语法、表达三类。表达继续复用现有知识项和复习调度，在 `vocabulary_details.pos` 中标记为 `expression`，并兼容此前的 `phrase`；不为它新建相同结构的数据表。句子讲解可把整句摘入表达。
- 原因：完整句子与可复用短语有独立浏览价值，但底层记忆状态、中文释义和例句结构与现有记录一致，当前无需承担数据库迁移和重复调度逻辑的成本。

## D-019：语法以结构公式为主键并分层保存说明

- 状态：已确认（2026-08-05，用户反馈）
- 决定：语法主标题固定为纯英文结构公式；中文标签、一句话用途、完整讲解、正例翻译、错例说明和易混提醒分别生成、校验并保存。列表只显示结构公式与一句话用途，学习卡和详情逐层展开其余内容。
- 兼容：Room v5 保留旧 `name` 列作为唯一键，新记录令其等于 `patternEn`；旧记录通过展示回退拆分混合标题，不要求清库。
- 原因：标题用于识别“形式”，一句话用途用于快速回忆，详细讲解用于理解；混用字段会让 AI 输出、去重、卡片布局和复习提示同时失真。

## D-020：听力训练只做核心循环，不落库

- 状态：已确认（2026-08-31，按 `英语听力训练模块DESIGN.md` §25 的 MVP 范围实现）
- 决定：听力训练做成「学习」下的第六个入口，一轮 10 句，只实现「只听声音 → 猜意思 → 揭晓 → 再听一次」这条循环。一轮的状态活在页面里，不进 Room，中途退出会明确提示这一轮会丢。
- 原因：文档 §25 自己把第一版限定在验证这个循环上；§14 的智能混合模式、§20 的听力能力画像、§23 的最强/最弱场景和 §24 的周对比全都需要跨轮次历史，没有历史就只能编数字。先不建表，等这个循环被真机验证过、确实要留历史时，再一次把表和统计一起做，比现在先加一张用不上的表划算。
- 明确没做：语音识别、Shadowing 自动评分、多口音、自由输入的 AI 评价、自我判断模式。
- 选项数量以设计稿为准：四选一（一个正确 + 三个干扰）。`英语听力训练模块DESIGN.md` §25 写的是「中文三选一」，设计稿屏 52 写的是「三个错误选项」，两份文档冲突时取更新的那份；四个选项也能容纳更多误听类型，区分度更好（2026-08-31，用户确认）。
- 对齐设计稿「实现规格」：主操作 56dp、固定在底部不随内容滚动；对错同时给图标和文字，不单靠红绿，正确用自定义语义色 `correct`；进度条带「第 n 题，共 N 题」的 contentDescription；播放键带 stateDescription。
- 未对齐且已知：设计稿要求学习流内 TopAppBar 左侧用 close 图标，现有单词/语法/阅读/朗读/情景五个页面用的都是 ArrowBack，只改听力反而不一致，留作全局统一时一起改。AI 生成的加载态设计稿要求「步骤清单而非纯转圈」，阅读页也还是转圈，同样留作统一处理。
- 相关取舍：
  - 第三级提示（挖空英文）由本地按 `keyExpression` 从原句生成，不问 AI——挖空位置必须和重点表达严格一致，交给 AI 会对不上。因此 `keyExpression.en` 必须原样出现在句子里，本地校验强制这一条。
  - 答错固定记 20 分，对应 §21 的「完整字幕以后才理解：20」；不记 0 分是因为用户确实在揭晓页读到了英文。
  - 影视和游戏场景走 §15 的 Inspired Scene：写风格相同的原创台词，不照搬真实作品原话，授权说不清的东西不批量进库。

## D-021：生成边写边用，不再整批干等

- 状态：已确认（2026-09-01，用户要求"优化整体生成速度"）
- 决定：长生成一律"先给能用的那部分"，而不是等整批闭合：
  - 听力一轮十句改成**逐条交付**。流里每闭合一个句子对象就解析、校验、送到界面，攒够 3 句直接开练，
    剩下的边听边补；用户答得比写得快时停在"下一句还在写"，而不是把这一轮当成做完了。
  - 单词学习在**复习到期卡片期间**就把新词先写好（到期卡片 ≥ 3 张才提前发），点"学新词"时内容已在手上。
    语法讲解 → 练习题此前已经是这个做法，这次把单词对齐过来。
- 原因：这些等待都发生在用户已经决定要学的时候，等待时间越长越容易在这一步流失；而内容本身是顺序可用的，
  没有理由攒够一整批才让人开始。
- 代价与边界：提前生成有可能白花一次调用（复习几张卡就退出）。所以只在"人已经明显投入了"的时机提前发，
  并且只发一次、结果缓存；点进去时正在跑就等它，不重复发。
- 明确没做：TTS 音频预取。合成器是单实例，边播边预取会互相打断，风险大于收益，留待需要时再单独设计。

## D-022：模型按功能分别指定

- 状态：已确认（2026-09-01，用户要求）
- 决定：每个 AI 调用带一个 `AiTask`（听力/阅读/单词/语法/翻译/情景/点词与提问/测评/发音提示），
  provider 按它取模型。设置页「各功能使用的模型」逐项指定，模型清单用 `GET /models` 拉回来供选择；
  没指定过的功能跟随默认模型，默认模型一改它们自动跟着改。
- 原因：听力那种一次写十句带干扰项的调用和"点个词马上要出字"的调用对模型的要求正好相反，
  一个全局模型必然在某一头将就。
- 取舍：偏好里只存显式设过的覆盖项，不给九项各存一份默认值——否则改默认模型要挨个改，
  还会把"跟随"和"恰好一样"两种状态混成一种。
- 界面是两级**独立页面**（`settings/models` → `settings/models/{task}`），不是弹窗：要翻的是
  几十上百行的模型清单，还要在九个功能之间来回比对，浮窗放不下也滚不顺（2026-09-01，用户要求）。
- 默认模型本身也在这个页面改（列表第一行），不再只能在 onboarding 里定。
- 清单按 id 关键词筛掉不能对话的模型（生图/嵌入/语音/重排/审核）。只是按名字猜，所以留
  「显示全部」的口子，全被筛光时原样列出——宁可多列一个，也不能把新模型藏起来。

## D-023：等待期间必须区分"没接通"和"模型在想"

- 状态：已确认（2026-09-01，用户反馈"听力的接通中要很久"）
- 决定：流式调用把进度分成 `Connecting` / `Thinking` / `Writing` 三段。响应头一到就离开
  "接通中"；推理增量单独收，服务商肯给文本就显示尾巴，不给就说一句"推理模型会先想一会儿"。
- 原因：默认模型是推理模型，开口前的思考期没有正文增量，界面一动不动，全被误报成"接通中"——
  用户会以为是网络问题去查网络，其实只需要等。这两件事该做的反应不一样，就不能混成一个字样。
- 顺带修掉：增量块的 `content` 为 `null` 时整块解析失败（不少服务商第一块就是这样），
  以及推理长度不计入输出硬上限的漏洞。
- 还没做：把 `reasoning_effort` 之类的参数发出去真正减少思考时间，需要先确认服务商认不认。

## D-024：先量再修——「接通中」到底卡在哪一段

- 状态：已确认（2026-09-01，用户反馈改完文案后「接通中」仍然最久）
- 背景：「接通中」这一段跨了 DNS、建连、TLS、上传请求、等服务端返回响应头五件事，
  光知道"一共很久"没法修，只能猜。所以先把它拆开量出来，再动手。
- 决定：
  - OkHttp 装一个 `HttpTimingListener`，响应头到达（或失败）时打一行分段耗时到 `LazyDogAI`；
    请求上带 op tag，日志能对上是哪个调用。复用连接时没有 DNS/建连两段，正好做对照。
  - 收到第一个增量时打一行「首个思考 / 首个正文」，把服务端的思考时间也量出来。
  - DNS 结果 IPv4 优先。IPv6 不通但有 AAAA 记录的网络上（国内移动网络常见），
    OkHttp 会先在 IPv6 上干等满 connectTimeout（10 秒）才轮到 IPv4，这十秒全在「接通中」里。
    OkHttp 4.12 还没有 `fastFallback`，所以自己排序；不丢 IPv6，IPv6-only 网络上 IPv4 会立刻
    unreachable，不耽误。
  - 「换用 `max_completion_tokens`」的结论存进偏好，按模型记住。之前每一次调用都要先用
    `max_tokens` 撞一个 400 再换名重发，白搭一整个往返，且每次都付。
  - 等待页显示已等秒数：干等时它是唯一在动的东西，也让"到底多久"变成一个能说出口的数字。
- 明确没做：把结论缓存在进程内的静态集合里。偏好里已经有一份，再留一份全局可变状态，
  两处口径迟早不一致（测试之间互相污染就是第一个信号）。

## D-025：等的是模型在想，不是网络

- 状态：已确认（2026-09-01，真机日志）
- 量出来的一次听力（gpt-5.6-terra）：DNS 32ms、建连 14ms、TLS 37ms、发请求 2ms，
  **等响应头 49038ms**，响应头到首个正文 650ms，写完 8025 字符又用了约 29 秒，合计 79 秒。
- 两个结论：
  1. 网络完全不是瓶颈（三段加起来 83ms），IPv6 那条猜错了，改动留着无害。
  2. **推理模型想完之前连响应头都不发**，即使请求里带了 `stream: true`。
- 决定：
  - 「在等模型」从**请求发出**那一刻算起，不再等响应头（`CallHooks.onRequestSent`）。
    否则整个思考期界面上都写着「接通中」——而真正的接通只有几十毫秒。
  - 每个 `AiTask` 带自己的 `reasoning_effort`：听力/阅读/单词/语法 low，情景/点词/发音 minimal，
    翻译判定和测评 medium（判定忽宽忽严比慢更伤）。这是唯一能真正砍掉那 49 秒的一刀。
  - 不认这个参数的服务端会回 400：**带着它挨了任何 400 都先去掉重试一次**（有的网关只回一句
    含糊的"参数不对"，认不出是哪个），但只有服务端点名说了这个参数才记进偏好——
    否则一次无关的 400 会把这个模型的思考力度永久关掉。
  - 代价：真正无关的 400 会多花一个往返才报错。可接受，报错路径本来就不是热路径。
- 查文档后修正（2026-09-01）：**现在 API 里已经没有非推理模型可挑了**。GPT-5.6 全家
  （Sol / Terra / Luna）都是推理模型，OpenAI 把推理和通用合并成一个模型加一个 effort 滑杆；
  GPT-4o / 4.1 / o4-mini 已于 2026-02-13 退役。剩下的非推理模型全是专用的（生图、语音、转写），
  正好是模型列表里被筛掉的那批。等价做法是 `reasoning_effort: "none"`。
- 同时修正一个已经写进代码的错：`gpt-5.6-terra` 支持 none / low / medium(默认) / high / xhigh / max，
  **不支持 minimal**，而上一版给点词、情景、发音提示设的正是 `minimal`——会挨 400，
  然后兜底把整个参数丢掉，回落到默认 medium，比不设还慢。

## D-026：思考力度是一串候选，而且用户能自己选

- 状态：已确认（2026-09-01，用户要求"令模型选择可以支持选 effort"）
- 决定：
  - 每次调用带的不是一个取值，而是**候选序列**：首选 + `low` 兜底。挨了 400 就退到下一个，
    退到底才不发。关键是别退回"不发"——那等于模型默认（多数 medium），比想要的还慢。
  - 只留一个兜底。候选越多，撞不上的模型要多花几个往返才安定下来；`low` 几乎所有推理模型都认。
  - 结论分两层记进偏好：「不认这个参数」和「拒过哪几个取值」。只有服务端点名了参数名、
    或点名了我们刚发的那个取值，才记进去。
  - 设置页每个功能可以自己选力度（跟随推荐 / 七个取值 / 用模型默认），和选模型放同一页——
    都是"这个功能怎么跑"，分两页要来回翻。
  - 层级修正（2026-09-01，用户反馈"层级怪怪的"）：**默认也是一项**，和九个功能一样能配
    模型和力度；每一项进去都是同一种页面，里面「模型」「思考力度」两个分区；
    选中即生效、不自动退出——原来选模型会跳走、选力度不跳走，用起来莫名其妙。
  - 力度也跟着走三层：这个功能设的 → 默认设的 → 都没设。**没设过时是 `none`（不思考）**
    （2026-09-01，用户定）：这个 App 的调用基本都是"按模板填内容"，思考换来的质量提升
    远不如它带来的等待明显（实测一次听力思考 49 秒、写 29 秒）。要质量的场合单独调高那一项。
    「各功能用自己的推荐值」保留成默认页里的一个显式选项，不再是隐含的初始状态。
  - 默认模型定为 `gpt-5.6-terra`（`LocalEnv.AI_MODEL`，模板同步改）。
  - "如果支持"不靠猜：模型拒过的取值标成"这个模型不支持"并禁选，整个参数被拒过的模型直接说明
    "选什么都不会发出去"。这些结论都来自真发过一次被顶回来的记录。
  - 用户选了具体取值时仍然保留 `low` 兜底：他选的那个不被支持时，退到别的取值也比退回默认强。
- 明确没做：换 Responses API。它能流式给出思考摘要（`response.reasoning_summary_text.delta`），
  但那只解决"看得见"，一秒都省不了；Chat Completions 的 delta 里没有任何 reasoning 字段，
  原始思维链任何接口都不给。要等真的需要"看见模型在想什么"时再迁。

## D-027：等 AI 的界面统一成一个等待区

- 状态：已确认（2026-09-01，用户要求"其他的 loading 页也优化优化"）
- 决定：所有长生成的进度回调从"字符数"换成"阶段"（`GenerationStage`），界面统一用
  `AiWaiting`，它一次回答三件事：在做什么（各页面自己说）、卡在哪一步（没接通 / 模型在想 /
  正在写）、等了多久。覆盖单词、阅读、语法讲解与出题、中译英出题与判定、情景演练、
  能力测评、发音提示——原来只有听力有。
- 原因：只报字符数的话，最花时间的那一段（推理模型开口前的思考，实测占六成）界面完全静止，
  看着像卡死；而"没接通"和"在想"用户该做的反应不一样，一个是查网络，一个是等。
- 顺带：情景演练和发音提示原来根本没有进度回调（非流式），这次一并接上流式，
  等待期间才有东西可报。
- 保留原样：已经能铺真内容的地方优先铺内容——语法讲解一有正文就显示正文，
  听力一攒够句子就显示"已经写好 N 句"。真内容永远比进度条好。
- 没动：数据库读取那几个转圈（单词/语法/阅读的 Loading），它们是本地查询，几十毫秒，
  加秒数和阶段只会更吵。

## D-028：拼写训练是独立入口，S0 并进单词流程

- 状态：已确认（2026-09-01，用户要求「实现新模块单词记忆」，按设计稿 59～64 屏重做）
- 背景：同一套规格有两份文档——仓库根目录的 `拼写训练DESIGN.md`（引擎与数据结构）和
  Claude Design 的设计稿 59～64 屏（界面与几条判定门槛）。设计稿自己的「待定问题」里
  留着三条，但屏幕和说明卡其实已经把其中两条答了，这次一并定下来。
- 决定：
  - **入口**：拼写是「学习」下的独立入口，不是单词流程里的一步。依据是设计稿顶栏——
    59/60/61/63 屏写的是自成一轮的「拼写练习 · n / 12」，而 62 屏写的是「新词 · 1 / 12」。
    只有 S0 接触（词块拆分）并进单词新词卡：那本来就是"第一次见到这个词"该看见的东西。
  - **手滑门槛**：编辑距离 1 且用时 < 2 秒。设计稿「阶段升降级规则」最后一行要求
    "单次手滑不降级"，待定问题里给的候选就是这个值，先按它实现。手滑不计连续错误
    也不降级，但仍然记一条 attempt——它是真发生过的数据，只是不该被读成"忘了"。
  - **Mastered 取宽松档**：间隔 ≥ 7 天。设计稿状态机卡写的是 7 天，§18 的严格档（14 天）
    留作以后想收紧时的旋钮。
  - **一张卡只算一轮复习**：多次提交都写拼写画像，但通用 `ReviewScheduler` 只在卡片翻篇时
    调一次。上一版每次提交都调，一张卡来回试三次就被记成三轮，`lapseCount` 会虚高。
  - **提示要到底也要留痕**：提示拉到第 5 级等于看了答案，这时补记一次零分提交。
    否则"一路要提示然后翻页"是一条完全不影响进度的绕路，练不练一个样。
  - **音形对应这一维由调用方声明**：引擎不猜题面是不是放了音，`audioPrompted` 显式传。
  - **提示梯度到第 5 级才给答案**（2026-09-01 用户反馈"提示应该聪明点，而不是直接泄露答案"）：
    `拼写训练DESIGN.md` §7 把 L4 写成"展示 Chunk：en + viron + ment"，但那已经把整个词拼出来了，
    和 L5"显示完整答案"没有区别，等于白白浪费一级。改成弱块留空的骨架 `en + _____ + ment`，
    对应 §3 S3 的写法。L2 给的也是挖过空的区域而不是原词，L3 的片段掐头去尾。
    这条是对 markdown 的**有意偏离**，有单测守着（`no hint below the last one spells the word out`）。
  - **局部补全的题面是逐字母格子**（2026-09-01 用户反馈"下划线间隔，而不是一整个下划线"）：
    按设计稿 63 屏，每个缺的字母各占一格、各有一条下划线，中间留空。格子直接是输入控件，
    底下不再摆第二个输入框——否则用户要在两个地方之间来回看。
  - **画像的错误率按"错误次数"做分母**，不是"答题次数"。用户想知道的是"我一错就错在哪儿"；
    用答题次数做分母的话，正确率一上升，弱点就被稀释得看不见了。
- 关于上一次 revert（fdc45fc）：那一版是照 markdown 猜 UI 写的，把拼写塞进了
  `WordStudyScreen` 并和词义复习交替。设计稿画好之后按设计稿重做，引擎和持久化那部分
  （`domain/spelling/`、Room v8、备份）复用了原实现。
- 明确没做：设计稿 §20 的扩展项（AI 生成干扰项、FSRS、手写/语音拼写、遗忘曲线）。
  干扰项目前由 `SpellingEngine.recognitionOptions` 本地按常见错法生成，够用且不花 token。

## D-029：熟词复习就是拼写，但入口不合并

- 状态：已确认（2026-09-01，用户问「单词复习是不是应该等于拼写」）
- 背景：上一版做完之后有两个拼写判官。单词页把 Familiar 以上的词翻成「给中文写英文」，
  判分走 `ProductionCheck`；拼写练习页判分走 `SpellingEngine`。两者互不知情：
  在单词页写对一个词，不进 `spelling_attempts`、不算错误类型、画像页看不见；
  而且两边对「差一个字母」的结论都不一样（一个算 Close/Hard，一个算错但可能是手滑）。
  更糟的是两个入口共用一套 `nextReviewAt`，而拼写队列不按到期筛选，
  练一轮拼写会把没到期的词也往后推——两件事其实早就在偷偷合并了，只是用了两套账。
- 决定：**合并判官，不合并入口。**
  - 单张卡的出题、提示梯度、判分、写复习时间收进 `feature/spelling/SpellingCardView`，
    单词页的产出卡和拼写练习页递进去的是同一个 `SpellingCard`。删掉 `ProductionCheck`
    及其测试——留着一个没人用的第二判官，迟早会被谁又接回去。
  - 产出卡按这个词**自己的拼写阶段**出题，不是一律完整默写。所以"熟词"不等于"直接默写"，
    拼写阶段还低的熟词会先考局部补全。
  - 入口不合并：生词仍然先考词义（还没记住意思的词谈不上默写），「表达」（整句短语）
    没法做字母级训练仍走四档自评，而「拼写练习」入口留给想集中练的时候。
    全部合并会把 10～15 分钟的日常循环撑重，跟产品前提冲突。
  - 队列闸门：拼写队列到期的先排完，不够一轮才拿没到期的补位；补位的
    `advanceReviewSchedule = false`，画像照记但不动 `nextReviewAt`。加练不该改写复习计划。
- 未验证：这条规则要落到 Room 才看得出效果，而项目还没有 Robolectric/instrumented 测试基础，
  所以只有引擎层有单测，仓储层这一段靠真机验收。

## D-030：拼写的起点按通用阶段推，复习不设「熟了才考」的门槛

- 状态：已确认（2026-09-01，用户实测反馈「下划线分字母没实现」「复习还是看词显示意思，没拼写检查」）
- 现象与原因：两条反馈是同一个根。
  1. `addVocabulary` 给每个新词抢先写一行 `SpellingProgress()`（stage = Seen），
     于是所有词的题型都是 S1 四选一，要连着答对两轮才升到 S2。逐字母下划线那几屏
     （设计稿 63）实际上要练两轮才见得到，用户合理地判断成"没实现"。
  2. D-029 把复习卡改成拼写卡，但门槛留在 Familiar 以上，也就是 `stability ≥ 7.0`；
     按 `SimpleIntervalScheduler` 要连着答对四轮才够。所以那条改动**一次都没触发过**，
     复习看起来毫无变化。
- 决定：
  - 起点交给 `SpellingEngine.initialStageFor(KnowledgeStage)` 统一推：
    Exposed → Seen、Learning → PartialRecall、Familiar → GuidedRecall、Mastered → FreeRecall。
    只往低了猜（通用阶段说的是认不认得），但不能一律从 Seen 起。
  - 不再抢先写空行；**判断"练没练过"看 `lastAttemptAt` 而不是看行在不在**。
    这样已经写进库的那些空 Seen 行也一并失效，老数据不用迁移就恢复正常。
  - 复习卡去掉「熟了才考拼写」的门槛：非新词、单个词形的都走拼写卡，
    难度由该词自己的拼写阶段决定。整句表达（`pos=expression`）仍走四档自评。
- 教训：状态机的门槛要放在**实际到得了**的位置上。Familiar 这道门在文档里读着合理，
  落到调度参数上是"连对四轮"，等于把功能关掉了。以后加这类门槛，先按调度算一遍要多久才够得着。

## D-031：目标和兴趣都可多选、可自填；批量记忆方法按记忆提示设计稿重写

- 状态：已确认（2026-09-01，用户反馈「主要目标和感兴趣话题可以多选，可以自己添加」
  「单词生成的怎么记还是太傻了」）
- 配置：`主要目标` 从单选改成多选，目标和话题都加了「自己加」——四个预置目标装不下
  「面试」「带娃看英文绘本」这类真实理由，而这个值唯一的去处就是进提示词。
  存储不动：`learning_goal` 仍是一个字符串，多个目标用 `、` 连起来（`LearningGoals`），
  老数据 split 出来就是只有一项的集合，不需要迁移。选择器抽成 `TagPicker`，
  onboarding 和设置页共用一个。
- 记忆方法：批量新词里的 `memoryHintZh` 之前是「30~70 字的记忆方法」，实际产出大量
  `联想：……那一整块可被控制的地盘，就是 territory。` 这种——把释义讲了两遍，
  没有任何能从中文回到英文词的线索。按 `词汇记忆提示DESIGN.md` §2.1/§9/§10 重写规则：
  必须以「构词：／同源：／词形：／发音：／对比：／搭配：／场景：／联想：／谐音联想：」之一开头，
  15~60 字，优先真实构词并带一个学习者认识的同根词搭桥（terr- 土地，如 terrain），
  提示词里把上面那句 territory 作为反例写进去，并要求模型自检「没见过这个词的人能不能顺着它想起这个词」。
- 校验只做窄的一层：`ContentValidation.isHollowMemoryHint` 挑出空话和过短的句子，
  命中就**把这条提示清空而不是丢掉整个词**——词还能学，提示留空后卡片上出现的是
  「生成记忆提示」，那条是按单词单独生成的结构化提示，质量本来就更高（§10 宁缺毋滥）。
  「这条联想有没有用」本地判不了，不去猜，免得误杀真拆对了词根的那种。
- 搭配短语：释义下面那几个搭配现在单击即朗读，中文翻译直接摆在英文下面。
  裸英文短语对初学者等于没写：既不知道怎么读，也不确定是什么意思。
  双击查词、三击讲整句的老交互不变，这里只是给一直没事做的单击补了个动作。
- 话题数量不设上下限（原来是 2~5 个）。上限本来是想防止兴趣太散导致选材没重点，
  但这是用户自己的事，卡在按钮上只会让人删掉一个真感兴趣的话题。
  下一步仍要求至少选一个——一个都没有的话选材就没有任何依据了。

## D-032：搭配的翻译跟着搭配一起生成，不点一次翻一次

- 状态：已确认（2026-09-01，用户反馈「搭配短语翻译一开始就一起生成，别点击再生成翻译」）
- 决定：`GeneratedWord.collocations` 从 `List<String>` 变成 `List<Collocation>`（`en` + `zh`），
  生成新词时一次要齐，入库时一起写进 `collocationsJson`。卡片进屏时翻译已经在那儿，
  不再是「点一下等一次网络往返」。
- 不做数据库迁移：`collocationsJson` 存的本来就是一段 JSON，
  `VocabularyJson.decodeCollocations` 同时认老的 `["curb traffic"]` 和新的
  `[{"en":..,"zh":..}]`，老行按「只有英文、还没翻译」处理，界面上仍可点开现翻。
  为一个字段写 Room 迁移不值得。
- 解析放宽：模型偶尔还是会照更常见的写法给一串裸字符串，
  `CollocationListSerializer` 把它当成只有 en 的对象，而不是整批词解析失败——
  一个字段的写法差异不该让十几个词连同拼写事实一起白生成。翻译为空在校验里也是允许的。

## D-033：记录里的单词点开是整页词卡，不是半屏抽屉

- 状态：已确认（2026-09-02，用户反馈「记录里点击单词，不出 modal，而应该是一个类似学习的时候的单词卡片的 full activity」）
- 决定：新增路由 `library/word/{itemId}` 和 `WordDetailScreen`，记录里的单词和表达都走它。
  半屏 `ModalBottomSheet` 原来要装释义、搭配、可重新生成的记忆提示、例句和复习控件，
  一直滑不说，还把「记录里的词」显得比「学习时的词」低一等——它们本来就是同一个词。
- 表达一起搬过来：它和单词存在同一张表，版面完全一样，只是不给记忆提示面板
  （那套提示按「一个词最值得记什么」设计，对整句无从谈起），朗读也按整句读。
  语法留在抽屉里：它没有词卡那套内容，独占一页是空的。
- 搭配卡片抽成 `feature/vocabulary/CollocationChip`，学习卡和词卡共用一份。
- 这一页不接摇一摇提问：`UI_BRIEF.md` §5.4 写明记录页不响应。它现在长得像学习页，
  但入口仍在记录里，先按文档走；要开的话是改文档、再包一层 `AskHost`。

## D-034：听力接上摇一摇；S0 进拼写练习；复习间隔按 §13 的阶梯走

- 状态：已确认（2026-09-02，用户问「摇一摇应用到拼写和听力里了吗」「S0~S6 有落实吗」，答案是各差一块，随后要求「都改掉」）
- 三件事：
  - **听力页的摇一摇原来是空壳**：`LazyDogApp` 包了 `AskHost`，但 `ListeningScreen`
    一处 `ProvideAskContext` 都没有，`canAsk` 恒为 false，传感器根本不注册，
    没传感器时也不会降级出顶栏问号。现在按揭晓前后给两份上下文，顶栏补上 `AskTopBarAction`。
    揭晓前那份一个英文单词都不给——这一页的立身之本是"英文永远最后出现"，
    提问入口不能成为绕过它的后门，有单测守着（`ListeningAskContextTest`）。
  - **S0 接触进拼写练习**，修正 D-028 的「S0 只并进单词新词卡」。原来
    `questionType(Seen)` 直接给四选一：一个词第一次露面就让人从四个拼写里挑，
    那是猜，不是接触。现在 S0 是一张不判分的接触卡（词形 / 读音 / 词块 / 例句），
    看完推到 S1 并落在阶梯最低档，本轮末尾以四选一的形式再来一次。
    单词页的复习卡仍然不出接触卡——那个词在新词流里已经见过，再"认个脸"是白走一趟，
    而且接触卡不判分，这一次复习就白复习了。
  - **复习间隔换成 §13 的固定阶梯**（10 分钟 / 1 / 3 / 7 / 14 / 30 / 60 天）。
    原来是 `current * 2` 这类连续公式，而且算出来的 `currentIntervalDays` 谁也没读，
    排期完全交给通用 `ReviewScheduler`——等于 §13 只写在文档里。现在档位和
    `nextSpellingAt` 都落库，拼写队列按它判到期。
- **10 分钟这一档落在会话里**：真等十分钟再弹一次不现实，也不是设计稿要的东西；
  它要的是"刚学完立刻答对不算记住"。所以落到最低档的词（接触完的、答错退档的、
  靠提示写对的）由 `SpellingScreen` 排回本轮队尾再考一次，每个词一轮最多一次。
- **通用 `nextReviewAt` 仍归 `ReviewScheduler`**，拼写阶梯不去改写它：一个是"这个词条
  什么时候再出现"，一个是"下次以多强的提示考写"，D-029 的队列闸门就是为了这个分工。
  所以队列里两个"到期"要分开：`dueNow`（拼写阶梯到期）只决定排在这一轮的哪个位置，
  `dueForReview`（词条本身到期）才决定 `advanceReviewSchedule`。合成一个的话，
  拼写到期而词条没到期的词答完就会把单词复习的排期往后推——正是那道闸门要挡的事。
  本轮末尾的延迟重考同理不推第二次（D-028「一张卡只算一轮复习」）。
- 数据：Room v11 重建 `spelling_progress`，`currentIntervalDays` 按 1 天 = 1440 分钟折成
  `currentIntervalMinutes`，已练过的词按「最后一次作答 + 原间隔」补出 `nextSpellingAt`，
  没练过的留空回退到通用复习时间——否则升级完一口气全部到期。备份里这两个字段带默认值，
  老备份照样能读（`ignoreUnknownKeys`）。
- 未验证：迁移本身没有测试基础（项目没有 instrumented / Robolectric 环境），
  升级路径要真机验收；十分钟档在一轮 12 词里的手感也要真人练一轮才知道会不会啰嗦。

## D-035：词条以原型入库，只对新词生效

- 状态：已确认（2026-09-02，用户提出「词汇应该保存原型，然后衍生出变形」，并明确范围：
  只有生成单词和双击查词两条入口生效，不做现存数据迁移）
- 背景：去重键是精确串匹配（`WHERE term = :term`），而双击查词入库存的是**句子里的表面形式**
  （`term = word`）——AI 其实在 `WordExplanation.term` 里返回了词条形式，我们没用。
  于是 `go` 和 `went` 各存一条，各自走一遍拼写 S0～S6、各攒一份画像：
  用户以为在练一个词，系统当成两个。`Went` / `went` 同理。
- 决定：
  - **term 存原型**，双击查词由新增的 `lemma` 字段还原，生成新词由提示词要求词典形式。
  - **只做词形还原**，不做短语归并（点 `gave` 不给 `give up`）、不做拼写纠正、判不出来就原样返回。
    还原必须结合句子（`saw` / `left` / `found` 离开语境无解），所以由模型做，**本地一行都不猜**——
    和 `SpellingFacts` 那条同源：猜出来的东西不能拿去当学习内容。
  - **查库查两次**：先按点到的形态查（命中就零成本），没命中才讲解，拿到原型再查一次。
    只查第一次的话，库里有 `go` 而用户点 `went`，会白花一次生成再存一条重复的。
  - **换了词形必须明说**：标题显示词条、底下一行"你点的是 went"、按钮写"把 go 放进生词本"。
    偷偷替换会让用户以为点错了行，或者以为存进去的词丢了。朗读仍读他点到的形态——
    他要听的是这句话里这个词怎么念。
  - **新增 `seenAs`**：例句存的是用户读到的原句，句子里出现的是变形。语境默写要挖的空是它。
    顺带修掉一个既有缺陷：`ClozeSentence` 原来按子串找，`run` 会命中 `runs`
    挖出 `___s every morning`；存原型之后 `go` 撞 `going` 会让它变常见。改成按词边界匹配。
- **改了主意的一处**：上一轮我提议唯一键取 `(lemma, pos)`，实际做的时候放弃了 pos。
  `explainWord` 根本不返回 `pos`，双击查词存进去的词性是空字符串；拿一个一半是空、
  另一半是自由文本（`v.` / `vi.` / `verb`）的字段做键，只会制造新的重复。
  代价是 `book`(书) 和 `book`(预订) 仍然合并成一条——那要靠 sense 层解决，不是 pos 能解决的。
- **明确没做**：变形表（过去式/进行时等）不存。规则变形（`walk → walked`）是词法规则不是词的属性，
  存下来是把同一条规则复制一百份；真正该存的是不规则变形，那要等 `inflectionsJson` 那一步，
  和拼写模块的 `SpellingErrorType.Morphology` 专项训练一起做才有意义。
- 未验证：`lemma` 的还原质量取决于模型，尤其是 `saw` / `left` 这类歧义词，要真机看一批。

## D-036：词条身份换成 (lemma, 词性)，多义词各自成条

- 状态：已确认（2026-09-02，用户给出 `单词记忆DESIGN.md`（Vocabulary Data Model Design）并要求落实）
- 背景：D-035 把词条身份从"句子里的表面形式"收敛到原型，但键仍然只是一个字符串。
  文档 §3 要的是 `Lexeme = lemma + language + POS`，§5 要的是一个词条下可以挂多个词义。
- 做了的（身份层）：
  - **词性收成封闭集合**（§2.2 Universal POS 风格）：`PartOfSpeech`，`wire` 存库过 AI、
    `labelZh` 上界面，老写法（`v.` / `vi` / `expression`）都能解析，认不出来的原样留着但进不了键。
    这直接解掉了 D-035 里"pos 是自由文本所以不能进键"那条限制。
  - **身份键 = (lemma, 词性)**：`record/NOUN` 和 `record/VERB` 各自成条。
  - **多义词各自成条**（§5、Principle 3）：同一个 (lemma, 词性) 下的第二个词义可以存，
    但要用户在查词面板上明说。不做中文释义的模糊比对——像不像和是不是同一个词义是两回事。
  - **不规则变形入库**（§4）：只存推不出来的（go → went/gone）。规则变形不存。
    立刻就有消费者：查词第一步本地按词形命中，点 `went` 直接找到 `go`，省一次生成。
- **明确没做，以及为什么**：
  - **frequency_rank / frequency_score / frequency_band**（§10）：本地没有任何语料来源，
    唯一的填法是让模型编一个"381"出来。那是把猜测当事实存进库，和 `SpellingFacts`、D-035
    一脉相承的那条底线冲突。有真实词频表再说。
  - **pronunciations 表 + audio_url + 英美双读音**（§8）：发音走系统 TTS，没有音频源；
    一张只有一行、`audio_url` 恒为空的表不解决任何问题。`record/NOUN` 与 `record/VERB`
    重音不同这件事，已经由"两个词条各有自己的 ipa"覆盖了。
  - **user_* 三张进度表拆分**（§18/§20/§21）：这是单用户 app，`user_id` 恒定；
    而且六维能力已经在 `spelling_progress` 里（`SpellingProfile`），
    再拆一层只是把同一份数据搬个家。
  - **lexemes / senses / examples / phrases 拆表**（§11、§51）：现在一行 `vocabulary_details`
    就是一个词义，例句和搭配也已经挂在词义上——文档 §6/§7/Principle 4 的目标其实已经满足，
    差的只是"同一个词条的多个词义之间的连线"，而那由 (lemma, 词性) 这个键就能查出来。
    真正拆表要重写所有词汇界面，收益在这一版体现不出来。
  - **§27～§50 的学习材料层**（Phrase / Idiom / PhrasalVerb / WordFamily / 易混词 /
    Memory Aid 类型 / Learning Package）：这一层和现有的 `词汇记忆提示DESIGN.md`
    有大面积重叠（记忆提示已经在按"这个词最值得记什么"选类型，正是 Principle 9/12 的意思），
    要先把两份规格对齐再动手，不能各修一半。
- 数据：Room v12 → v13，`vocabulary_details` 加 `formsJson` / `senseOrder`，
  并把老数据的词性规范化（`v.` → `VERB`、`expression` → `PHRASE`）——那是同一个值换个写法，
  不是语义迁移，词条一条没动。

## D-037：学习材料层的两份规格合并，记忆材料分词条级和词义级

- 状态：已确认（2026-09-02，用户要求「照着你的判断解决冲突，然后修改设计」）
- 背景：`单词记忆DESIGN.md` §27～§50（学习材料层）和 `词汇记忆提示DESIGN.md` 覆盖同一片地方，
  一份从"库里该有哪些表"往下看，一份从"给用户看什么才记得住"往上看。后者已经实现
  （`MemoryType` 七类、`MemoryAssistance`、记忆提示面板），前者是新写的目标形态。
  两份不对齐的话，下一次改动会各修一半。
- 裁决（细则写在 `词汇记忆提示DESIGN.md` §16，模型文档对应章节加了指回去的裁决说明）：
  - **记忆类型以七类为准**，§35 的十三类映射进来（root / prefix_suffix / etymology → MORPHOLOGY，
    visual / association / story → VISUAL_ASSOCIATION，semantic_scene / context → CONTEXT，
    spelling_pattern → ORTHOGRAPHY）。细分不进实现：模型要在十三个标签里挑，界面上分不出差别。
  - **固定优先级降级成打平时的兜底**。§35 给了 1..10 的排序，但那和"不强制统一记忆法"
    （提示设计 §2.1、模型设计 Principle 9）冲突。模型仍然自选，只有选不出时才按那个顺序。
    唯一的硬规则是中文谐音永远垫底——两份文档在这件事上本来就一致。
  - **记忆材料分两层挂载**（§34 的 lexeme_id / sense_id，这次唯一改变现有行为的一条）：
    构词、词形、发音挂词条，场景、对比、搭配、联想挂词义。多义词各自成条之后（D-036），
    不分层的话 `run` 的五个词义会各自生成一遍一模一样的构词和拼写提示。
  - **不另开 `spelling_aids` 表**（§42）：这份数据已经有两个准确的家。§42 真正的贡献是
    "记忆拆分 ≠ 真实形态学"那条警告，吸收进提示设计 §3.1——`territory → terr + itory`
    能帮上忙，但不能以"词根"的名义给出去，用户会把编的拆分当知识记住。
    这和拼写引擎"本地猜出来的词块不能拿去讲『这里最容易错』"（D-028）是同一条原则。
  - **不存 AI 自评分**（§46 的 quality_score / confidence）：那是让模型给自己的作业打分，
    再把 0.91 当事实存库。采纳 generator / generator_version——代码里本来就有 model 和
    promptVersion，留下来才能在换模型后批量重刷旧内容。
  - **关系表等消费方**（§32/§33）：易混词先留在记忆提示里。关系表的价值是双向可查和
    专项训练，在那之前一张只写不读的表只会留下悬空引用。但补一条约束：confusions 里的词
    必须够格进库，不能为了凑三个列一个用户永远遇不到的词。
  - **Phrase 的 lexeme_id 改为可选**（§28）：Principle 8 已经满足（表达是独立条目、独立进度），
    但演练里存的整句没有母词，必填只会制造错误归属。
- 顺带修的：`单词记忆DESIGN.md` 这次更新覆盖掉了原来的拼写 S0～S6 规格，而代码和文档里有
  13 处引用指着它。原内容已从 git 恢复成 `拼写训练DESIGN.md`，引用全部改指过去。
- 只改了文档，没动代码。下一步的实现顺序建议：先做 §16.2 的两层挂载（它省 token、也去掉
  多义词之间自相矛盾的提示），再考虑 §16.4 的 generator 版本。

## D-038：语法点按「大类 + 归一化公式」判重；记忆材料分两层生成

- 状态：已确认（2026-09-02，用户："思考语法的去重要咋做，数据结构咋设计"，并授权直接实现）
- **语法去重**：原来是 `patternEn` 精确串匹配，等于没有去重。同一个现在完成时，模型这次写
  `have/has + past participle`、下次写 `has/have + p.p.`、再下次写 `present perfect`，
  三条都能进库，然后各自排一遍复习——用户会看到三张讲同一件事的卡。
- 查了业界做法：Cambridge 的 **English Grammar Profile**（1200+ 条语法能力描述，按
  SuperCategory / SubCategory + CEFR 等级组织）是这件事的权威参照，结论是
  **身份必须用受控词表，自由文本只配拿来显示**——和这个项目刚给词性做的那套是同一个套路。
- 决定：身份键 = `大类 + 归一化公式`（`domain/grammar/GrammarPoint.kt`）
  - 大类取封闭集合 20 个（照 EGP 的 SuperCategory 划分，只取 A1～B2 常用的那层）。
    **不照抄 EGP 的 1200 条**：那份数据我们手上没有，硬编一份编号只会得到一堆对不上号的假 id。
    将来拿到数据集，可以把这 20 类映射过去，这是留的升级路。
  - 归一化：同义写法映射 + 去标点虚词 + token 排序拼接，另加一张常见时态名的别名表
    （`present perfect` / `现在完成时` → 和它的公式同键）。
  - **只做等值判断，不做子集判断**：`will + base verb` 是
    `if + present simple, will + base verb` 的子集，但它们是两个语法点。差一点就合并错。
  - 大类是必需的第二维：`was/were + verb-ing` 和 `am/is/are + verb-ing` 归一化后完全相同，
    只有大类能把过去进行时和现在进行时分开。
  - 老数据的键在第一次判重时由 `backfillGrammarKeys()` 补算，不写进 SQL 迁移：
    归一化规则在 Kotlin 里，迁移脚本抄一份，两处会慢慢跑偏。语法表只有几十行，补算一次就够。
- **记忆材料两层生成**（落实 D-037 里那条唯一有行为改变的裁决）：生成一个词义的记忆提示前，
  先看同一个词条的别的词义有没有写过构词 / 易错段 / 发音；有就直接沿用，并在提示词里
  明确让模型这三项填 null、只写这个词义自己的东西。回来之后再以已有的那份覆盖一次——
  模型不听话时也不让两个词义各说一套。`run` 的五个词义因此只生成一遍词条级材料。
- 另外删掉 `单词模型DESIGN.md`：它是 `单词记忆DESIGN.md` §1～§26 的早期子集，两份并存
  只会让人改错地方。
- 未验证：语法大类的选取质量取决于模型（`时态` 这种中文回答会被判无效并丢掉整条），
  要真机看一批；两层挂载要有一个多义词真的生成过两次提示才看得出效果。

## D-039：朗读播放的五条不变量

- 状态：已确认（2026-09-04，用户实测"单词没声音"→ 连续多轮真机日志定位）
- 背景：单词朗读在 MIUI 上时有时无，"前两次能读、第三次就哑"。查下来根本不是 Azure 那边的
  问题——日志里 `收到 985ms、写入 985ms` 说明音频完整拿到也完整写进了 AudioTrack，
  但一个字都没响。问题全在 `PcmAudioPlayer` 怎么使唤 AudioTrack。
- 决定（改这个文件前必须先看这五条）：
  1. **等合成期间不开声。** `begin()` 只清场，第一块语音到手才垫前置静音并 `play()`。
     对着空缓冲 `play()` 会被 AudioFlinger 立刻判 underrun 并把 track 停掉；被停掉的 track
     既不消费数据、播放头也不动，日志里是 `track disabled due to previous underrun`。
  2. **热路径不 `flush()`。** javadoc 明说 flush 会把还没播的数据直接丢掉，而且它是异步落地的：
     曾经在"裁掉多余静音"时 pause+flush，紧接着写入的语音被一起扔掉，表现就是
     "数据全写进去了、一点声音都没有"，日志特征是 `AudioTrack 写入中断：ret=0`。
  3. **正常播完用 `stop()`，打断才用 pause+flush。** `stop()` 在 MODE_STREAM 下会把已写入的
     数据播完再停。收尾时刻是估算的，估早一点点很正常——用 flush 就等于剪掉尾音，
     用户实测是"结尾仓促、有时没读完就断"。
  4. **不信 `playbackHeadPosition`。** underrun 之后它归零且不再推进。拿它算缓冲余量会让补数据的
     一方以为"还满着"而停手，拿它算剩余会永远等不完——实测一次朗读卡在 drain 里 12 秒。
     播放进度一律用"开声时刻 + 已写入时长"推算。
  5. **收尾期间保持写入。** 被 underrun 停掉的 track 只有下一次写入才会重启，最后一块语音之后
     再没有写入的话，剩下的音频就烂在缓冲里。`drain` 期间持续补一小块静音。
- 另外：路由判断改用 `track.routedDevice`。原来用 `getDevices(GET_DEVICES_OUTPUTS)`，
  它列的是所有**已连接**的输出设备，蓝牙耳机连着但声音走外放时会误判成蓝牙，
  白垫 800ms 静音还让 keep-alive 线程对着扬声器空转。
- 教训（比结论更值钱）：这一轮里有三次"看起来合理的结论"是错的——
  "是音色合不出音频"、"是一次点击触发了多次朗读"、"是 AUDIO_END 说明播放结束了"。
  每次都是靠加日志把盲区补上才推翻的。**这块代码不要靠推理改，靠日志改。**
  现在 `AzureSpeech` 这个 tag 下的日志（请求编号、首块音频耗时、垫了多少静音、
  收到/写入/总耗时）就是为此留的，不要顺手删。

## D-040：保留 Dragon HD 音色，不做"读到哪句高亮哪句"

- 状态：已确认（2026-09-04，用户："算了别做高亮了，不能牺牲HD"）
- 需求：阅读页朗读全文时高亮当前句。
- 技术结论：**Dragon HD 音色不支持 `<bookmark>`**（官方文档
  "Supported and unsupported SSML elements for Azure Speech HD voices"：bookmark 在
  DragonHD 和 DragonHDOmni 下都是 No）。没有书签事件就拿不到"第几句从音频第几毫秒开始"，
  高亮无从对齐。WordBoundary 事件只有 DragonHDOmni 支持，当前音色也没有。
- 可行的三条路和为什么不走：
  - 换标准 Neural 音色（支持 bookmark，语速也能生效）——**牺牲 HD 音质，用户明确否决**。
  - 换 DragonHDOmni + WordBoundary——换了音色，同样是牺牲当前音质；且字符偏移是相对 SSML 的，
    还要还原 XML 转义才能对回原文。
  - 一句一句分开合成——每句都要重等一轮网络往返（实测首块音频 300～900ms），
    句子之间会有明显停顿，朗读体验的损失比高亮的收益大。
- 决定：功能撤回，代码已回退（切句、书签 SSML、跟随协程、UI 高亮参数全部删除）。
  什么时候值得重开：Azure 给 DragonHD 补上 bookmark 或 word boundary 支持，
  或者本地做单词/句子音频缓存之后可以接受一句一合成。
- **顺带发现的既有缺陷**：HD 音色同样不支持 `<prosody>`，所以**设置里的"慢速/正常/快些"
  对当前音色一直是无效的**，服务端直接忽略。这不是本次改动引入的，已记入 `ROADMAP.md`
  的已知限制，怎么处理（换音色 / 去掉设置项 / 只在非 HD 音色下显示）等用户定。

## D-041：架构边界写成硬约束，默认变更预算为 0

- 状态：已确认（2026-09-04，用户提出 agent 长期改项目会把架构改形的问题）
- 问题：这个项目长期由 agent 增量修改。agent 最常见的失败不是写错代码，而是为了局部方便
  一点点把架构改形：多出一套平行抽象、领域逻辑漏进 UI、绕过已有接口。单条任务看都合理，
  攒起来架构就散了。只靠 `AGENTS.md` 里的软性叮嘱挡不住。
- 决定：在 `ARCHITECTURE.md` 新增 §0「架构约束（硬约束）」，明确依赖方向、模块边界、
  禁止事项、复用优先级、架构变更预算（默认 0）、动手前的影响分析和收工前的漂移自查。
  `AGENTS.md` 的实现流程相应加入这两步。
- 刻意没写进去的：**不写"禁止 feature 之间互相引用"**。现状里
  `feature/ask/AskHost`、`feature/grammar` 等本来就被多个页面复用，写一条立刻被现状打脸的
  规则，只会让后面的 agent 学会忽略这份文档。改为约束"只能引用对方的公开入口，
  不得把别人的私有实现改成 public"。
- 已知不足：文档仍是软约束（§0.7）。真要钉死边界，下一步是把 §0.1 的两条 grep 做成
  构建期检查或 ArchUnit/Konsist 测试，让违反直接构建失败。引入测试依赖本身要走 §0.5，
  所以先不做。

## D-042：单词用标准 Neural 的播音腔，例句和短文保持 HD

- 状态：已确认（2026-09-04，用户："单词朗读用普通 Neural，播音腔"）
- 决定：`SpeechStyle.Word` 把音色换成同一个人的标准 Neural
  （`en-US-Ava:DragonHDLatestNeural` → `en-US-AvaNeural`，`Ssml.kt` 的 `broadcastVoiceOf`）；
  `SpeechStyle.Sentence` 仍用用户在设置里选的音色。
- 原因：
  - Dragon HD 是奔着对话调的，念一个孤立单词会自动补上句子的情绪和句末降调，听着像在跟你聊天
    而不是在报读音。用户实测同一个词连点三次，三种语气——HD 的 prosody variation 是设计如此
    （官方文档把"每次输出略有变化"列为特性）。查词要的是词典示范音：平、清楚、每次一样。
  - 附带解决半个 D-040 遗留问题：HD 不支持 `<prosody>`，语速设置对它无效；标准 Neural 支持，
    所以**单词的语速从此真的生效**。例句和短文仍然无效，留在 `ROADMAP.md` 的已知限制里。
- **这是恢复一条被误判推翻的旧设计。** 早先代码里有过同样的映射，后来因为"单词点了没声音、
  例句正常"被删掉，当时的结论是"这条 SpeechConfig 的连接绑在 HD 后端上，点名标准 Neural
  合不出音频"。D-039 证明那个结论是错的：音频其实完整合出来也完整写进了 AudioTrack，
  是播放侧的 flush 把它扔了。音色是无辜的。
- 因为那个怀疑没有被真机反证过，`speak()` 留了一条退路：换过的音色返回 `Failed` 时，
  用用户选的音色重念一遍，并打一条 W 日志（`$voice 合不出音频，退回 $configured`）。
  宁可没有播音腔，也不能让用户点了没声音。这条日志出现就说明当年的怀疑是对的，
  那时再考虑给标准 Neural 单开一个 `SpeechConfig`。
- 选音色的策略放在 `AzureSpeechProvider.speak`，不放回 `buildSpeechSsml`：退回原音色需要用
  原音色再排一次版，策略埋在排版函数里就没法退。`buildSpeechSsml` 保持"照给的名字排版"。
- 待真机确认：单词能出声（说明标准 Neural 在这条连接上没问题）、语气比 HD 平、
  连点几次语调一致、慢速/快些确实听得出差别。

## D-043：播放状态收归一个 PlaybackController，按钮不再各存各的

- 状态：已确认（2026-09-04，用户新增 `语音服务DESIGN.md` 并要求实现）
- 按 §0.5，新抽象要先提变更请求。这一条就是那份请求的落档：设计文档由用户提出，
  实现也由用户直接授权，所以直接记成已确认，不再空转一轮。
- 问题：朗读一直是"点了就 `scope.launch { speak(...) }`"，没有任何播放状态。
  点下去到出声之间没有反馈，看起来像没点上；点第二下不会停，只会再念一遍；
  快速点几个词，最后响的是哪一个取决于回调赛跑。
- 现有架构的限制：`SpeechController` 是"发起一次朗读"的入口，没有、也不该有跨按钮的
  界面状态；`PcmAudioPlayer` 的令牌只解决"底层同时只有一段音频"，解决不了
  "界面上是哪个按钮在播、点第二下应该停还是重播"。这层状态没有归属方。
- 决定：`core/speech/PlaybackController` 作为播放状态的唯一拥有者，对界面暴露
  `IDLE / LOADING / PLAYING / ERROR` + `sourceId`；按钮统一用 `SpeakButton` 按自己的
  sourceId 取状态。交互是"同源再点=停、异源=顶替、连点取最后一次"，
  正确性靠 generation + jobId 校验而不是 cancel（迟到的 Azure 回调照样会到）。
- 考虑过的替代方案：
  - **把状态塞进 `SpeechController`**——它现在是无状态的转发层，塞进去等于让同一个类
    既管 SDK 生命周期又管界面状态，而且没有事件队列，三个线程会并发改它。
  - **每个页面自己管**——现在就是这样，问题正出在这里；一页多个播放按钮时必然出现
    两个按钮同时显示在播。
- 影响范围：`core/speech` 加三个文件，`core/designsystem` 加一个按钮；
  12 个页面的播放调用改走控制器；`SpeechProvider.speak` 加一个可选回调参数
  （加可选参数不算改语义，§0.3）。无数据迁移。
- **和 D-039 的分工**：D-039 管的是"音频怎么才能真的响"（AudioTrack 那五条），
  这一条管的是"界面上哪个按钮在响"。控制器不碰 `PcmAudioPlayer`，
  底层那五条不变量一行没动，`AzureSpeech` 的日志也原样留着。
- 未验证：真机观感——起播快时 LOADING 转圈会不会闪一下、蓝牙下起播是否照旧。
  单测覆盖的是状态机本身（切换、连点、迟到回调、失败重来），8 条。

## D-044：要变速就换标准音色，不留"点了没反应"的按钮

- 状态：已确认（2026-09-04，用户在"下一件做什么"里选了这条并采纳推荐方案）
- 问题：D-040 顺带发现 HD 音色忽略 `<prosody>`，D-042 把单词换成标准 Neural 之后单词的语速
  生效了，但**例句、阅读全文、以及听力页的「慢速重放」仍然无效**。听力那个尤其糟：
  按钮明写着"慢速"、界面还告诉用户这一遍怎么算分，点下去和正常速一模一样。
- 决定：音色策略从"按内容类型分"扩展成"按内容类型 + 这次要不要变速分"，收进
  `core/speech/Ssml.kt` 的 `voiceFor(configured, style, rate)`：
  - 单词 / 搭配 → 标准 Neural（播音腔，D-042 的理由不变）
  - **任何非正常语速** → 标准 Neural（否则 prosody 被忽略）
  - 其余（正常语速的例句、短文）→ 用户设置的音色，默认 HD
- 取舍：变速的那一句音色会变——听力里"再听一次"是 HD、"慢速"是标准音色，听得出来。
  接受这个代价，因为"慢速"本来就不是为自然度服务的；反过来，一个明确操作毫无反应
  是更严重的问题。设置页的语速项在非正常语速时会写明"变速时换用标准音色"。
- 考虑过的替代方案：
  - **去掉慢速入口**——听力训练里"听不懂就放慢一点再听"是真实需求，砍掉是回避问题。
  - **只在设置里说明语速只对单词有效**——那听力页的慢速按钮还是假的，等于把问题留给用户记。
  - 全部换成标准音色——例句和短文平时听的就是 HD 的自然度，没必要为了一个偶尔用的功能整体降级。
- 影响范围：`Ssml.kt` 加一个纯函数，provider 改一行调用，设置页副标题改一行。无数据迁移。
  换过的音色万一合不出音频，`speak()` 原有的退回逻辑照旧生效（宁可没变速也不能没声音）。
- 未验证：真机上听力"慢速"是否真的变慢、音色切换是否突兀。

## D-045：进步证据从既有学习事件推，不为它新开一张表

- 状态：已确认（2026-09-04，用户提出 `持续学习DESIGN.md` 并要求"分 milestone，尽量对齐设计"）
- 背景：那份文档是产品策略不是单个功能，37 节里六成已经散在 M1～M13 里。所以先在
  `ROADMAP.md` 立了 M14 并按文档自己的 P0/P1/P2 拆成 M14.1～M14.6，本条是 M14.1 的落档。
- 决定：今日战报（§14.1）和活跃度（§7.1）**不新增表、不改 schema**，
  全部从 `learning_events` 推：
  - 新学 = `activity='create'`，回忆 = `activity='review'`，
    想起来 = `rating != Forgot`（Hard 是费劲想起来了，仍然算成功提取）
  - "上次没想起来、今天想起来了" = 该知识点今天之前最后一次复习是 Forgot、今天做对了
  - 活跃天数 = 事件时间戳按本地时区去重
- 为什么不新开一张 `daily_stats`：那等于把同一件事记两遍。事件表已经是事实来源，
  再存一份汇总只会在补写失败、时区变化、恢复备份时和事实对不上，而且以后改口径
  （比如把 Hard 算成没想起来）要重算历史。推导出来的数永远和事件一致。
- 代价：每次进首页要拉最近 120 天的事件在内存里算。个人自用 App 这个量级（几千行）
  可以接受；真到了不能接受的那天，加缓存也比现在就加表强。
- 界面上的两处刻意选择：
  - **连续天数今天还没学不算断**，从昨天往回数。零点归零是在为难人，不是记录事实。
  - **「今天到这里」和「再学几分钟」是平等的两个按钮**，不是灰色小字。低门槛的前提是
    收工也算数（§6），做成让人有负罪感的样子就等于换个说法继续催。
- 明确不做（记在 ROADMAP M14 的"明确不做"里）：§27/§28 的社交、联赛、货币、商店；
  §30～§33 的埋点、指标体系和 A/B 实验——单用户跑不出统计显著性，埋点只会变成没人看的数据。
- 未验证：真机上首页的观感，尤其是战报卡在只学了一两个词时会不会显得空。
  单测覆盖的是口径本身（翻身判定、今天没学不算断、遇见不计入战报），13 条。

## D-046：难度偏置只准把题变难，变简单靠提示阶梯

- 状态：已确认（2026-09-04，M14.2）
- 问题：`持续学习DESIGN.md` §11 要求把成功率维持在 75%～85%，太顺就加难、太吃力就降难。
  加难那一半是直接的；**降难那一半有陷阱**。
- 陷阱在哪：拼写的阶段升级看 `stageSuccessCount`——这一档答对几次就往上走。
  如果"最近吃力"就把 FreeRecall 换成 PartialCompletion，用户答对的是更浅的题，
  升级却照记，掌握度会建立在更弱的证据上。**降难度会污染记忆模型**，加难度不会
  （更难的题答对了，本来就足以证明更浅那一档）。
- 决定：
  - 偏置只作用于"这一次问什么"，**只能往难了走**（Recognition → PartialCompletion →
    ChunkRecall → GuidedRecall → FreeRecall），S0 接触卡和最难那一档不动。
  - 太吃力时题型不变，改成**提示阶梯从第 1 级起步**（首字母直接给）。
    这条路是诚实的：`masteryCredit` 本来就按提示层级打折，用了首字母答对只拿 0.8 分，
    掌握度自己会慢下来。既减轻了当下的挫败，又没有虚报会了。
  - 偏置不参与判分、不参与阶段升降。
- 落点：`domain/progress/Difficulty.kt`（成功率与偏置，纯函数）、
  `SpellingEngine.questionType(progress, difficulty)` 和 `startingHintLevel(...)`。
  拼写练习和单词复习共用 `SpellingCard`，所以两个页面一起生效。
- 可解释性（§11）：偏置不是 Steady 时题面上挂一行说明。不解释的话，用户感觉到的
  是"这个 App 今天忽然刁难我"，而不是"它发现我最近答得很稳"。
- 一轮开始时取一次，中途不变——做到一半换题型会让人以为是 bug。
- 未验证：真机上偏置的触发频率是否合理（阈值 90 / 65 是照文档取的，没有真实数据校准过），
  以及连续几天吃力时首字母是不是给得太频繁。

## D-047：换成 FSRS，存量数据不迁移

- 状态：已确认（2026-09-04，M14.3；"存量数据不管"是用户的明确决定）
- 问题：原来的 `SimpleIntervalScheduler` 按四档评分乘固定倍数（Good ×2.5）。它看不见
  **这次复习到底有多难**：一个刚复习完两天的词和一个搁了三个月才想起来的词，
  后者说明记忆牢固得多，间隔该拉得更开，但两者都只是 ×2.5。
- 决定：实现 `FsrsScheduler`（`ReviewScheduler` 接口不变），用官方默认参数。
  - 间隔由稳定度和期望保留率（0.9）推出，`DECAY = -0.5` 时间隔约等于稳定度。
  - 三条关键性质：隔得越久才想起来涨得越多（提取难度效应）、已经很稳的词涨得越少、
    难的词涨得慢。这三条正是原来看不见的。
  - 忘了：稳定度掉下来但不清零；仍然 10 分钟后重学，保留原有体验。
- **存量数据不迁移**：老词的 `stability` 本来就以天为单位、`difficulty` 本来就是 1~10
  （当初写 `MemoryState` 时就是照 FSRS 的字段留的），FSRS 直接读得懂。
  下一次复习起自然按新算法走，不需要 SQL 迁移，也不会出现"一觉醒来两百个词突然到期"。
  代价是过渡期里老词的 stability 是旧算法给的，和 FSRS 的口径不完全一致——
  复习几次就自然收敛过去了，不值得为它写一次性重算脚本。
- 旧实现和它的测试一并删掉：留着一个不再被调用的调度器，下一个人只会疑惑该改哪个。
- 不做参数优化：FSRS 的 optimizer 要成百上千条复习记录才有意义，现在跑只是拟合噪声。
  期望保留率也先不做成设置项，用 0.9 跑一阵再说。
- 未验证：真机上间隔的实际手感。单测钉的是性质不是数值（间隔单调、提取难度效应、
  边际递减、难词涨得慢、忘了不清零、存量状态读得懂），11 条。

## D-048：每周自动休一天，中断回来不补债

- 状态：已确认（2026-09-04，M14.4；宽容口径由用户在三个方案里选定）
- 问题：`持续学习DESIGN.md` §7.2 要求"一次中断不应摧毁长期积累"，§26 点名不要对
  回来的用户说"你已经落后 74 个复习"。原来两条都没有：连续天数缺一天就归零，
  中断一周回来首页照样把到期量摊在脸上。
- 决定一：**每周自动允许休一天**。连续天数回溯时，两次"休息"至少隔七天；
  连着缺两天就是真的断了。
  - 否决"冻结券 / 补签券"：那是要攒、要花、还要有界面管的虚拟货币，
    和 §27 的奖励优先级（能力反馈 > … > 虚拟货币）正好相反。个人自用 App 更没有理由
    把宽容做成一件需要经营的事。
  - 否决"不做宽容只看周完成率"：断掉那天的挖心感仍然在，而那天恰恰最需要一个回来的理由。
  - 实现上有个坑：**走到头的那次休息不能算**。否则"今天学了、昨天和前天都没学"会显示成
    "连续 1 天 · 休过 1 天"——那次休息什么都没保住。只有后面还接着学习日的休息才计数。
- 决定二：**断三天以上回来走热身流程**（§26）。今天只排一步，文案是"不用补以前的，
  今天先热身几分钟"，**不显示积压数量**。
  - 附带一个事实：换成 FSRS 之后本来也不该补。过期越久可提取性越低，到期是连续的，
    不像固定间隔那样堆成一笔必须还清的债。
- 决定三：**疲劳只用两个信号**（§25）——今天已经做够多（≥12 次提取）、且最近六次里错了三次。
  其余信号要么没在记（跳过、退出次数），要么需要个人基线才有意义（反应时间：
  慢下来多少算慢，得先知道这人平时多快）。宁可信号少而准：误判成"你累了"然后劝人收工，
  比不判更糟。累了的时候今天也只排一步。
- 招呼优先级：中断 > 疲劳 > 照常。刚回来的人第一句该听到"欢迎回来"，不是"你看起来有点累"。
- 未验证：三天这个门槛（`COMEBACK_AFTER_DAYS`）和疲劳的两个阈值都是拍的，
  要用一阵子才知道会不会太灵敏。

## D-049：长期证明指到具体那一次错误，配不出来就不说

- 状态：已确认（2026-09-04，M14.5 的第一条）
- `持续学习DESIGN.md` §14.3 要的是"30 天前你还会在这里出错，今天对了"。
  这句话的说服力全在**具体**上——用户会认出自己当时写的那个错拼法，
  那一刻不需要任何数字。换成"你的拼写正确率从 76% 涨到 82%"就完全是另一回事了。
- 数据现成：`spelling_attempts` 存了每次作答的 `expected`、`answer`（真的写成什么）、
  `correct`、`hintLevel`。所以不用加表，两条窄查询就够。
- 三条口径：
  - 最近那次必须是**无提示**写对（`hintLevel == 0`）。提示答对不足以证明会了。
  - 旧错误至少 21 天前。太近了拿出来说，用户自己都还记得，反而显得这个 App 在凑数。
  - 空答案不能当证据：交白卷不是"写错了"。
- 挑跨度最大的那一条：三个月前的错误比三周前更值得说。
- **配不出来就什么都不说**。没有证据时硬挤一句"你进步了"，会让别的反馈跟着一起贬值。
- M14.5 另外两条没做，原因不同：Proof of Progress 挑战（§15）要一次新的 AI 调用加契约、
  校验和一个听力挑战页，是完整功能不是收尾；能力地图第二层（§16）卡在听力一轮不落库
  （M11 的明确决定），连读 / 数字这些维度的正确率现在根本没存。
- 未验证：21 天和"最近 7 天内写对"这两个窗口是拍的，真机上要看多久能出现一条证明。

## D-050：例句贴兴趣，但硬塞比无关更糟

- 状态：已确认（2026-09-04，M14.6）
- `持续学习DESIGN.md` §19.2 的意思是例句应该长在用户自己的生活里：技术用户该看到
  "The server went down during deployment."，而不是 "Tom bought an apple."。
- 现状是半截的：兴趣标签早就有（引导页的九个预置项），但它只影响**选哪些词**
  和阅读/听力的取材，不影响**例句怎么写**。于是词是贴着兴趣挑的，句子还是通用教科书句。
- 决定：`exampleSentenceRules` 收下兴趣列表，新词生成和双击查词的讲解都传进去。
- **同时加一句"这是偏好不是硬要求"**。这句话不是客气，是必须的：模型很愿意服从指令，
  不给退路就会把词硬塞进兴趣场景，写出"The motorcycle brewed a decision."这种句子。
  生硬的例句比无关的例句更糟——无关的至少还是句正常英语。
- 领域接口 `explainWord` 加了一个带默认值的 `topics` 参数（§0.3：加可选参数不算改语义）。
- 提示词测不了"句子写得好不好"，所以单测钉的是"该说的说了、原有约束没被挤掉"：
  兴趣出现在提示词里、退路那句在、等级和"绝不能编造出处"仍然在。
- 未验证：真机上例句是否真的变得贴近，以及会不会出现同一个领域反复出现的单调感。

## D-051：进步挑战不凑数、不计分

- 状态：已确认（2026-09-04，M14.5 的第二条）
- `持续学习DESIGN.md` §15 要的是让用户**亲耳听见**自己变强了：拿两到四周前学的几个词
  组一句话，他听懂了，这个结论是他自己的耳朵得出的，不是 App 给的一个分数。
- 三条设计上的选择：
  - **凑不够 4 个就不出这一轮**，界面直说"再过些日子来"。用刚学的词凑数，
    凑出来的"证明"证明不了任何事，还会让以后真正的证明一起贬值。
  - **没有分数、没有计时、没有连击，播放次数也不计分**。这一关证明的是"听懂了"，
    不是"一遍就听懂了"。加上计分就变成了又一个练习页。
  - **干扰项从用户自己的词库里随机取**，不让模型编。模型编的干扰项经常荒谬到能一眼排除，
    那样的关键词识别不听也能做对（和拼写四选一同一个教训：本地生成的干扰项常常荒谬到不会拼也能排除）。
- 校验最要紧的一条：**每个词都必须真的在句子里**，缺一个就整条失败重来。
  少用一个词，"你听懂了 4 个旧表达"这句结论就是假的。
- 实现时踩到一个真问题：`ContentValidation.exampleContainsTerm` 是按**整词**匹配的，
  拿它查 `end up` 永远查不到——而 `end up`、`supposed to` 这类短语恰恰是这一关最想考的东西。
  多词短语现在拆开逐词校验，代价是不校验词序。另外词库里存着整句表达（`pos=expression`），
  拿一整句话去组一句话是荒唐的，所以候选只要三个词以内的词条。
- §15 的"复述"没做：它和现有的朗读评估（`assessReading`）是同一件事，再实现一遍
  只会多一处录音权限流程。听懂 + 认出关键词已经足够支撑这一关的结论。
- 模型档位复用 `AiTask.Words`：都是"写一句自然的英文"，不值得为它在设置里多摆一个选项。
- 未验证：真机上模型能不能稳定地把四个不相干的词写成一句自然的话——
  这是整个功能最可能翻车的地方，失败会表现为"挑战句没通过校验"。

## D-052：听力作答落库，一轮的临时状态仍然不落

- 状态：已确认（2026-09-04，用户问"为啥听力不落库"后授权实施）
- 这是一条 `ARCHITECTURE.md` §0.5 要求的架构变更请求（表结构变更），补记如下。
- **问题**：`持续学习DESIGN.md` §16 的能力地图第二层要按听觉难点给正确率
  （连读 43%、数字 95%），但听力的作答从来没存过——`ListeningAnswer` 只活在页面的
  `remember` 里，退出这一轮就没了。同样被这件事卡住的还有 M11 里搁置的智能混合模式
  （70/20/10 需要弱项历史）和听力周对比。
- **现有架构的限制**：`listening_materials`（v15）存的是**句子**：原文、场景、播放次数、
  生成参数，服务于"最近材料"重听和下一轮去重。它回答不了"这个人在连读上栽过几次"——
  那是作答的属性，不是材料的属性。往材料表上挂正确率也不对：同一句可以被不同的人、
  在不同状态下答很多次。
- **提议的改动**：新增 `listening_attempts`（Room v15 → v16，只加表不动老表，自动迁移）。
  每题确认答案时写一行：归一化原句、对错、播放次数、最高提示级、这句的听觉难点、
  答错时栽在哪类误听、得分。**不设外键、冗余存一份原句和难点标签**——
  和 `drill_mistakes` 同一个理由：材料被删之后，"你在连读上栽了几次"这个事实仍然成立。
- **考虑过的替代方案**：
  - 把统计塞进 `listening_materials` 的额外列——同一句可以答很多次，一行存不下；
    而且材料是可以被清理的，作答历史不该跟着一起没。
  - 复用 `learning_events`——那张表是绑在 `knowledge_items` 上的（有外键），
    而听力句子不是知识项，硬塞进去要么造假知识项，要么把外键去掉，两个都更糟。
- **影响范围**：新表一张、备份格式 `schemaVersion` 3 → 4（旧备份缺这一项解成空列表，
  恢复后画像从零重新攒）、听力页多一次写入、多一个听力画像页。没有数据迁移风险，
  老库升上来就是一张空表。
- **仍然不落的东西**：一轮的临时状态（进行到第几题、选了什么、提示开到第几级）。
  中途退出照样丢，页面也照样明确提示这一轮会丢。要做恢复得再存"这一轮进行到哪儿"，
  那是另一件事，也还没有需求方。
- 画像口径上有一条容易搞反：**用了提示答对仍然算答对**。提示的代价已经体现在分数里，
  在画像里再罚一次等于同一件事扣两遍；要看真实听力，看"裸听一遍就懂"那个数。
- 未验证：真机上练几轮之后，听觉难点标签的分布是否够集中——如果 AI 每句都标一堆
  各不相同的标签，这一页会碎成一堆"还只练了 1 次"，那就得回头收敛标签集合。

## D-053：拼写提示改成题面上的骨架，判定反馈不再占提示等级

- 状态：已确认（2026-09-04，用户实机反馈"拼写的提示系统太智障了"，并授权我定方向）
- 病在哪（四条，都是实机能看出来的）：
  1. 提示是**第三人称旁白**——"开头那块是 sep"——用户还得自己在脑子里拼回单词里。
  2. 每要一次提示**弹一次 ModalBottomSheet**，要三次就弹三次，每次还得点「知道了」关掉。
  3. **还没交答案时无话可说**，第 1 级只能讲"一共 11 个字母，可以分成 3 个词块"。
     更糟的是 D-046 的 Easier 偏置正好让吃力的用户**从第 1 级起步**，
     于是他一进来看到的第一句就是这句废话。
  4. 和题型重复：局部补全题的题面本来就是挖空，提示再说"开头那块是…"等于说两遍。
- 决定：提示从"弹窗里的一句话"改成**输入框上方常驻的一行骨架，逐级显形**。
  五级是：词块结构 → 每块首字母 → 读音 → 薄弱段 → 答案。骨架从第 0 级就在，
  字母数本身是信息，不单收一级的费。
  - 按钮上写明**下一级会给什么**（「提示 · 首字母」）：代价是掌握度，
    总得让人在花掉之前知道买的是什么。
  - 第 3 级给的是另一个维度（音形对应），不是更多字母。
  - 第 4 级的薄弱段优先按**这次错在哪**定位，没有新错答案才退回历史统计。
- **「你错在哪一类」不再占提示等级**。它是判定反馈不是提示：知道"该双写"往往就够自己
  找出来了，为这句话收一级的费，等于逼人用掌握度买一个本来就该告诉他的判定结果。
- **Mastery Credit 仍然等距**（1.0 / 0.8 / 0.6 / 0.4 / 0.2 / 0）。重新审过一次：
  有人会觉得"看几个字母"和"看整块"不该扣一样多，但 Credit 衡量的是**这次提取有多独立**，
  不是泄露了多少字母。等距简单、可解释、和界面上"一级一格"的观感一致；要改成非等距，
  得先有数据说明某一级明显被高估或低估，凭手感调只会让分数更难解释。
- 旧的 `hintText` 和它的两个私有工具一并删掉，不留第二套说法。
- 未验证：真机上骨架那行的字号和 ` · ` 分隔在长词上会不会折行；以及吃力时从第 1 级起步
  （直接看到词块结构）是不是比原来那句废话真的有用。

## D-054：文章先值得读，学习目标让位；标题党和套路开头是硬拒绝

- 状态：已确认（2026-09-04，用户导入 `引人入胜的阅读材料DESIGN.md` 并要求实现，M15.1）
- 那份文档的原则 1 写得很直白：**把英语学习功能全删掉之后，文章还值不值得读，不值得就不该发。**
  原来的提示词是反过来的——先交代学习目标、再顺带要求自然，出来的东西大概率是
  "某某现象很常见，原因有三"。
- 决定：
  - 写作要求排在词汇要求前面，词汇那段明写"会破坏自然度就换说法"（§7）。
  - **每篇指定一种写法**（10 种 archetype，§6），按最近用过的轮换。只做主题去重挡不住
    "十篇一个样"——真正让人腻的是结构和句式，十篇不同主题的"Why X is actually Y"
    仍然是十篇同一个东西（§20）。全用过一轮就重来，宁可重复一个老写法，
    也不能因为"都用过了"退回没有写法约束的自由发挥。
  - **`readerPayoff`**（§4）：一篇只承诺一个收获，读完显示「值得记住的一件事」。
    本地硬校验四条：非空、不超长、不是标题的复述、不是"原因有很多"这类套话。
  - **标题党和套路开头是硬拒绝，不是警告**：`You Won't Believe` 这类标题、
    `In today's world` 这类开头一律整篇作废重来。骗到的点击会连着损伤对后面每一篇的信任，
    而这个 App 里"下一篇值不值得点开"就是全部。
- 篇幅从 160 词提到 300：160 词装不下 Hook → 好奇缺口 → 逐步揭示 → 兑现，
  只够写成一段说明文。300 也正好对上 §7.1 的配比（300~500 词配 4~7 个新词）。
- 存储：Room v17 给 `reading_materials` 加 `readerPayoff` 和 `archetype`（都带默认值，
  自动迁移）；备份升到 5，旧备份缺这两项解成空串，老文章照样能读，只是没有那张收获卡。
- 没做（记在 ROADMAP M15.2/M15.3）：Interest Critic 与无聊检测、Feed 卡片、行为反馈闭环、
  Topic Ranker。明确不做：Fact Pack 与来源核验、embedding 推荐、bandit、完整行为埋点——
  前两个要联网检索，后两个要成规模的行为数据，个人自用 App 都没有。
- 未验证：真机上模型会不会为了满足 archetype 而写得用力过猛（尤其"反直觉"和"小案例"
  这两种最容易编）；以及 300 词会不会让今日任务里那 4 分钟的阅读步骤变紧。

## D-055：阅读校验失败自动定点修一次，Critic 只能改善、不能弄丢合格原稿

- 状态：已确认（2026-09-04，用户反馈阅读生成后的校验失败明显增加，并要求继续 M15）
- 原因：M15.1 同时增加了 payoff、文章结构和逐字依据约束，但生成器仍沿用“一次返回不合格就失败”的
  老路径；这和 `ARCHITECTURE.md` §7 写的“否则修复或重试”不一致。约束本身是在防止假题、假引文和
  空泛文章，不能为了降低报错率直接删掉。
- 决定：第一次解析/schema/业务校验失败时，把**具体原因 + 原草稿**交回同一 Writer 定点修一次；
  修复后完整重跑本地校验，仍失败才把两次原因一起显示。引文比较只放宽排版等价项（大小写、弯引号、
  长横线、连续空白），不接受正文里不存在的句子。
- M15.2 的 Interest Critic 是独立调用，按设计阈值低分时最多重写一次。Critic 失败、返回坏分数、
  或重写稿破坏了学习目标时，一律保留已经通过本地校验的原稿；不能为了“更有趣”牺牲事实一致性和成功率。
- Feed 和反馈：Room v18 保存 teaser/category/completed/liked/saved，全部带默认值；备份格式升到 6。
  只记录完成、喜欢、保存三个明确意图，不引入滚动深度等单用户没有统计意义的埋点。

## D-056：阅读提问携带全文，题目在作答前给出原文锚点

- 状态：已确认（2026-09-04，用户反馈摇一摇缺少阅读原文、理解题不知道在问哪一处）
- 阅读材料是摇一摇上下文的唯一全文例外：默认阅读上下文和答完题后的题目上下文都携带完整
  `阅读原文`。仍使用现有结构化 `AskContext`，不截屏、不带知识库或其他页面内容。
- 理解题的 `evidenceFromText` 从“答完后的解释”前移到选项之前，并根据正文分段显示
  「题目对应原文 · 第 N 段」。只有确实存在于正文的依据才展示，避免把模型幻觉当原文。
- 新生成的每道题都要求尽量给依据句；`form` / `reference` 继续做非空和正文子串硬校验，
  全文大意题允许没有单句锚点。这样改善定位，同时不重新增加一类整篇拒绝条件。

## D-057：文档分层、索引与版本由 DOCS.md 统一维护，并进 CI

- 状态：已确认（2026-09-06，用户要求"维护所有文档并建立清晰的文档层级关系"）
- 起因：根目录 17 份文档平铺，最大的两份加起来超过 12 万字。新 agent 无从判断该读哪几份、
  哪份权威更高、哪份写的是目标形态而不是现状——`单词记忆DESIGN.md` 和 `拼写训练DESIGN.md`
  各自在开头手写了一段"实现口径"来打补丁，正说明缺一个统一的位置。
- 建立 `DOCS.md` 作为文档地图：L0 入口 / L1 工作规则 / L2 产品定义 / L3 技术契约 /
  L4 专项设计 / L5 过程记录六层。**层级表示作用域和阅读顺序，不表示权威高低**，权威顺序
  单列（架构硬约束 > 已确认决定 > AGENTS 规则 > 产品与设计稿 > 专项设计 > 路线图）。
- 写死两条最容易被违反的规则：专项设计写了不等于已实现（判断现状看 `ROADMAP.md` 的勾选
  状态和本文件）；代码与文档冲突时先停下来说明，不边写边绕。
- 每份文档加 YAML frontmatter（`tier` / `status` / `version` / `updated` / `authority`），
  版本用"主.次"：口径变更进主版本，补充澄清进次版本，纯排版两个都不动。
- 版本统一从 **1.0 基线**起算。不追认既往修改次数——git 里查得到的是提交次数，不是口径变更
  次数，拿它当版本号只会造一个看着精确、其实没根据的数字。`updated` 取 git 历史里该文口径
  最后一次变更的日期。
- frontmatter 与 `DOCS.md` §6 版本表由 `tools/check_docs.py` 强制一致，并新增 `Docs` 工作流
  在 CI 里跑。不靠"记得同步"——否则索引会比它索引的文档烂得更快。
- 新增 `CLAUDE.md` 作为 Claude Code 的会话入口，只转发到 `AGENTS.md` 和 `DOCS.md`。
  规则不复制第二份：两份规则最后一定会说不一样的话。
- **文件不移动、不改名。** 这 17 份文档被约 70 个 Kotlin 源文件按裸文件名引用（共 231 处），
  移进 `docs/` 或改成英文名要连带改源码注释，风险和收益不成比例。层级用索引和 frontmatter
  表达，不用目录结构表达。这一条是权衡后的取舍，不是遗漏；将来真要移动，先把源码引用一起改。

## D-058：`DESIGN.md` 改名为 `UI_BRIEF.md`（D-057"不改名"的唯一例外）

- 状态：已确认（2026-09-06）
- 问题：根目录的 `DESIGN.md` 是**给设计 agent 的界面任务书**，但仓库里还有七份
  `xxxDESIGN.md` 的模块专项设计。同一个名字指两类完全不同的东西，D-057 只能靠在索引里
  写一条"命名陷阱"来提醒——那是给缺陷贴标签，不是修掉它。
- 决定：`DESIGN.md` → `UI_BRIEF.md`，用 `git mv` 保留历史。
- 这是 D-057"文件不移动、不改名"的**唯一例外**，因为代价和那条结论的前提不同：
  D-057 拦的是 231 处引用的批量改名，而这一份全仓库只有 20 处引用（6 个 Kotlin 源文件
  共 7 处、6 份文档共 13 处），一次改干净。其余 16 份维持原名不变。
- 全部引用已同步更新，包括源码注释里的 `DESIGN.md §5.4` / `DESIGN.md 屏 19` 一类写法；
  章节号和屏号没有变，只换文件名。
- `持续学习DESIGN.md` 的一级标题原本写着"英语学习持续性专项设计（DESIGN.md）"，
  会被读成它就是那份任务书，一并去掉括号。
- 兼容性：不留转发文件、不留旧名软链。老聊天记录里的 "DESIGN.md" 指的是 `UI_BRIEF.md`，
  这一条写进 `DOCS.md` §3 的 L2 一节；留一个空壳文件只会让"哪个才是真的"这个问题一直在。
