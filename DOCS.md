---
doc: "DOCS.md"
tier: "L0 入口"
status: "生效"
version: "1.17"
updated: "2026-09-07"
authority: "文档清单、层级、权威顺序与版本表；本仓库文档元信息的唯一事实来源"
index: "DOCS.md"
maintenance: "改本文须同步 DOCS.md 的版本表，校验命令 python tools/check_docs.py"
---

# 文档索引

本文件是**仓库文档的地图**：有哪些文档、每份管什么、什么时候读、冲突了听谁的、现在是哪一版。

任何 agent 在开工前只需要读两份：`AGENTS.md`（规则）和本文（去哪儿找剩下的）。其余文档按需加载，不要一次性全读——最大的两份加起来超过 12 万字，全读只会挤掉真正要干的活。

## 1. 三十秒版本

| 我要做的事 | 先读 |
| --- | --- |
| 任何改动，开工前 | `AGENTS.md` |
| 想知道现在做到哪一步了 | `ROADMAP.md`（勾选状态）+ `DECISIONS.md`（最近的 D-0xx） |
| 加功能、改范围 | `PRODUCT.md` → 对应的 L4 专项设计 |
| 动 `app/` 的结构、加表、加依赖 | `ARCHITECTURE.md` §0（硬约束）→ §3～§5 |
| 改提示词、改 AI 的入参出参 | `AI_CONTRACTS.md` → 对应的 L4 专项设计 |
| 改界面 | `UI_BRIEF.md` + 设计稿 `懒狗放洋屁 MVP.dc.html` |
| 交接给下一个人或下一个 agent | `HANDOFF.md` |
| 做完了要写点什么 | 见 §5 文档维护规则 |

## 2. 层级

层级表示**作用域和阅读顺序**，不表示权威高低。权威顺序单独见 §4。

```text
L0 入口        README.md · CLAUDE.md · DOCS.md
                 │
L1 工作规则     AGENTS.md · HANDOFF.md          ← 约束 agent 怎么干活
                 │
L2 产品定义     PRODUCT.md · UI_BRIEF.md · 懒狗放洋屁 MVP.dc.html
                 │                               ← 定义做什么、长什么样
L3 技术契约     ARCHITECTURE.md · AI_CONTRACTS.md
                 │                               ← 定义怎么搭、怎么和 AI 说话
L4 专项设计     9 份 *DESIGN.md / CEFR 评测设计
                 │                               ← 单个模块的目标形态
L5 过程记录     DECISIONS.md · ROADMAP.md       ← 记录实际决定和实际进度
```

## 3. 文档清单

### L0 · 入口

| 文档 | 作用 | 什么时候读 |
| --- | --- | --- |
| [README.md](README.md) | 项目是什么、怎么构建运行、CI 怎么出 APK | 第一次进这个仓库 |
| [CLAUDE.md](CLAUDE.md) | Claude Code 的会话入口，只做转发，不放规则 | 自动加载，不用手动读 |
| [DOCS.md](DOCS.md) | 本文。文档地图与版本表 | 开工前，和 `AGENTS.md` 一起 |

### L1 · 工作规则（强制）

| 文档 | 作用 | 什么时候读 |
| --- | --- | --- |
| [AGENTS.md](AGENTS.md) | 强制工程规则、Git 方式、实现流程七步、完成定义 | **每次开工前，无例外** |
| [HANDOFF.md](HANDOFF.md) | 交接模板、设计↔编码交接要求、阻塞处理 | 任务结束、中断或被阻塞时 |

### L2 · 产品定义

| 文档 | 作用 | 什么时候读 |
| --- | --- | --- |
| [PRODUCT.md](PRODUCT.md) | 产品定位、目标用户、每日闭环、功能需求、MVP 范围与非目标、成功标准 | 加功能、判断某个需求算不算范围内 |
| [UI_BRIEF.md](UI_BRIEF.md) | **界面任务书**，不是某个模块的设计文档。信息架构、必须设计的流程、交付物、气质与文案 | 做界面、做设计交接 |
| `懒狗放洋屁 MVP.dc.html` | 设计稿本体（64 屏）。界面细节的最终依据，见 §4 | 实现某一屏之前 |

> `UI_BRIEF.md` 原名 `DESIGN.md`，2026-09-06 改名（`DECISIONS.md` D-058）：它是**给设计 agent 的界面任务书**，和 `拼写训练DESIGN.md` 那七份 L4 专项设计根本不是一类东西，同名骗过不止一个 agent。**看到老文档或聊天记录里的 "DESIGN.md" 指的就是本文。**

### L3 · 技术契约

| 文档 | 作用 | 什么时候读 |
| --- | --- | --- |
| [ARCHITECTURE.md](ARCHITECTURE.md) | §0 是**架构硬约束**（依赖方向、禁止事项、漂移自查八条），§1 之后是现状描述：技术栈、分层、包结构、数据模型、复习调度、阅读管线、服务与密钥、测试策略 | 动结构、加表、加依赖、加模块之前 |
| [AI_CONTRACTS.md](AI_CONTRACTS.md) | AI 是不可靠外部依赖的处理原则、Provider 边界接口、各类生成的请求/输出契约、本地校验、请求上限、失败与日志 | 改提示词、改结构化输出字段、加新的 AI 调用 |

### L4 · 专项设计（单模块目标形态）

这一层描述**应该长成什么样**，不描述**现在是什么样**。判断现状见 §4 的第二条规则。

| 文档 | 管什么 | 对应里程碑 |
| --- | --- | --- |
| [持续学习DESIGN.md](持续学习DESIGN.md) | 产品策略层：进步证据、每日最低目标、动态难度、提示阶梯、Mastery 模型、中断宽容、疲劳检测 | M14 |
| [引人入胜的阅读材料DESIGN.md](引人入胜的阅读材料DESIGN.md) | 阅读材料的价值模型、Hook→Gap→Discovery→Payoff 结构、题材引擎、生成管线、Interest Critic | M15 |
| [母语阅读DESIGN.md](母语阅读DESIGN.md) | 中文母版 + 英语语义片段替换：比例与配比、Span 类型、替换规则、点击与救援、学习闭环、向全英阅读迁移 | M19 |
| [英语听力训练模块DESIGN.md](英语听力训练模块DESIGN.md) | 听力的六维句子模型、裸听流程、四级提示、Listening Score、场景分类 | M11 |
| [拼写训练DESIGN.md](拼写训练DESIGN.md) | S0～S6 状态机、升降级规则、提示梯度、八类拼写错误、薄弱片段、延迟回忆 | M12 |
| [单词记忆DESIGN.md](单词记忆DESIGN.md) | 词汇数据模型：Lexeme / 词形 / 词义 / 例句 / 搭配的分层与建表建议 | M1、M3 |
| [词汇记忆提示DESIGN.md](词汇记忆提示DESIGN.md) | 七类记忆策略、记忆提示的提示词与输出结构、质量过滤、与拼写系统联动 | M13 |
| [单词视觉记忆图片DESIGN.md](单词视觉记忆图片DESIGN.md) | 按**词义**找配图：可视觉化判定、检索词生成、Brave Image Search、候选过滤与排序、复习撤图、版权口径 | M20 |
| [语音服务DESIGN.md](语音服务DESIGN.md) | Azure Speech 的 TTS / STT 集成、播放状态机、并发不变量、按钮交互规则 | M5 |
| [CEFR_英语能力评测与个性化学习系统设计.md](CEFR_英语能力评测与个性化学习系统设计.md) | 短题量自适应摸底、评分、分技能画像、由 CEFR 派生个性化词汇 | M2、M3 |

**L4 内部的两条已裁决冲突**，不要重新发明：

- `单词记忆DESIGN.md` §27～§50 的学习材料层与 `词汇记忆提示DESIGN.md` 大面积重叠，以后者 §16 的裁决为准。
- `拼写训练DESIGN.md` 与设计稿冲突时以设计稿为准（`DECISIONS.md` D-028）。

### L5 · 过程记录

| 文档 | 作用 | 什么时候读 |
| --- | --- | --- |
| [DECISIONS.md](DECISIONS.md) | 已确认决定 D-0xx。**推翻旧决定时保留旧记录并注明替代关系**，不要删 | 想做一个"看起来文档没写"的选择之前 |
| [ROADMAP.md](ROADMAP.md) | M0～M15 的里程碑、逐条勾选状态、明确不做的事、已知限制 | 想知道某功能做没做、发现新需求要记账时 |

## 4. 冲突裁决顺序

两份文档说法不一致时，从高到低：

1. **`ARCHITECTURE.md` §0 架构硬约束**。任何其他文档都不能推翻它，只能通过架构变更请求修改它本身。
2. **`DECISIONS.md` 中状态为「已确认」的最新决定**。它记录了对下面所有文档的**有意偏离**（例如 D-028、D-035～D-037、D-047）。
3. **`AGENTS.md` 的强制工程规则**。
4. **`PRODUCT.md`（范围与验收）与设计稿 `懒狗放洋屁 MVP.dc.html`（界面细节）**。界面细节与 L4 专项设计冲突时以设计稿为准。
5. **L4 专项设计**。
6. **`ROADMAP.md`**。它排顺序、记状态，不定义口径。

另外两条规则，比上面的顺序更容易被违反：

- **L4 写了不等于已经实现。** 专项设计是目标形态。要判断现状，读 `ROADMAP.md` 的勾选状态加 `DECISIONS.md`，再去看代码。不得因为某份 L4 描述了某功能就断言它存在。
- **代码与文档不一致时，先停下来说明，不要边写边绕。** 要么改代码对齐文档，要么改文档并补一条 `DECISIONS.md`，不要留下第三种事实。

## 5. 文档维护规则

### 5.1 什么时候必须动文档

| 你干了什么 | 必须更新 |
| --- | --- |
| 完成里程碑中的一条 | `ROADMAP.md` 勾选 + 写清没做的部分 |
| 做了一个文档没写过的选择 | `DECISIONS.md` 追加 D-0xx |
| 改了 AI 的入参、出参或提示词口径 | `AI_CONTRACTS.md` |
| 加表、加层、改依赖方向 | `ARCHITECTURE.md`，并先走架构变更请求 |
| 发现新需求但这次不做 | `ROADMAP.md`，不要顺手实现 |
| 改了任何文档的口径 | 该文 frontmatter 的 `version` / `updated` + 本文 §6 版本表 + §7 变更日志 |

### 5.2 每份文档的 frontmatter

所有根目录文档第一行起是一段 YAML frontmatter：

```yaml
---
doc: "ARCHITECTURE.md"
tier: "L3 技术契约"
status: "生效"
version: "1.0"
updated: "2026-09-04"
authority: "架构硬约束、分层与依赖方向、包结构、数据模型、复习调度、服务与密钥"
index: "DOCS.md"
maintenance: "改本文须同步 DOCS.md 的版本表，校验命令 python tools/check_docs.py"
---
```

- `tier`：所属层级，取值见 §2。
- `status`：`生效`（描述的就是当前口径）/ `部分落地`（目标形态，落地情况见 `ROADMAP.md`）/ `已归档`（保留供追溯，不再作为依据）。
- `version`：`主.次`。**主版本**在口径变更、契约字段变更、决定被推翻时 +1，含义是"读过旧版的人必须重读"；**次版本**在补充、澄清、修正笔误时 +1。
- `updated`：口径最后一次变更的日期。纯排版、错别字修正不动这个字段。
- `authority`：一句话说明本文是什么的唯一事实来源。

两份追加型记录（`DECISIONS.md`、`ROADMAP.md`）照同一套规则走：追加一条 D-0xx 或勾掉一条里程碑算次版本 +1，推翻一条已确认决定算主版本 +1。

### 5.3 版本一致性校验

frontmatter 和 §6 的版本表必须一致，由脚本强制：

```bash
python tools/check_docs.py
```

它检查四件事：每份文档都有 frontmatter；字段齐全且 `status` / `tier` 取值合法；frontmatter 与 §6 版本表逐字段相等；§6 表里没有指向不存在的文件。CI 的 `Docs` 工作流会跑同一条命令。

## 6. 版本表

以本表为准。改动文档后同步这里。

| 文档 | 层级 | 状态 | 版本 | 口径更新于 |
| --- | --- | --- | --- | --- |
| `README.md` | L0 入口 | 生效 | 1.2 | 2026-09-06 |
| `CLAUDE.md` | L0 入口 | 生效 | 1.0 | 2026-09-06 |
| `DOCS.md` | L0 入口 | 生效 | 1.17 | 2026-09-07 |
| `AGENTS.md` | L1 工作规则 | 生效 | 1.2 | 2026-09-06 |
| `HANDOFF.md` | L1 工作规则 | 生效 | 1.1 | 2026-09-06 |
| `PRODUCT.md` | L2 产品定义 | 生效 | 1.0 | 2026-09-03 |
| `UI_BRIEF.md` | L2 产品定义 | 生效 | 6.0 | 2026-09-07 |
| `ARCHITECTURE.md` | L3 技术契约 | 生效 | 1.5 | 2026-09-07 |
| `AI_CONTRACTS.md` | L3 技术契约 | 生效 | 9.0 | 2026-09-07 |
| `CEFR_英语能力评测与个性化学习系统设计.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-08-05 |
| `单词记忆DESIGN.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-09-02 |
| `词汇记忆提示DESIGN.md` | L4 专项设计 | 部分落地 | 5.0 | 2026-09-07 |
| `拼写训练DESIGN.md` | L4 专项设计 | 部分落地 | 2.0 | 2026-09-07 |
| `引人入胜的阅读材料DESIGN.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-09-04 |
| `母语阅读DESIGN.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-09-07 |
| `英语听力训练模块DESIGN.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-08-31 |
| `语音服务DESIGN.md` | L4 专项设计 | 部分落地 | 1.0 | 2026-09-04 |
| `持续学习DESIGN.md` | L4 专项设计 | 部分落地 | 1.1 | 2026-09-06 |
| `单词视觉记忆图片DESIGN.md` | L4 专项设计 | 部分落地 | 1.1 | 2026-09-07 |
| `DECISIONS.md` | L5 过程记录 | 生效 | 6.1 | 2026-09-07 |
| `ROADMAP.md` | L5 过程记录 | 生效 | 2.3 | 2026-09-07 |

设计稿 `懒狗放洋屁 MVP.dc.html` 不参与版本表：它是导出的设计产物，版本由导出时间决定。

## 7. 文档变更日志

### 2026-09-07（撤掉 term 唯一索引，判重只留一份口径）

- DECISIONS 6.1 追加 D-073：`vocabulary_details.term` 的唯一索引降为非唯一（Room v21 → v22），
  判重只在 `KnowledgeRepository.addVocabulary` 按 (lemma, 词性) 做。
  D-036 处加注说明它当初漏了这一半，旧记录保留。
- 只是补齐 D-036 已确认的范围，不改变身份口径；无新依赖、表、层次或依赖方向变化。

### 2026-09-07（谐音先贴近读音，再做诙谐画面）

- DECISIONS 6.0 的 D-072 替代 D-069 宽松近音部分，旧记录保留并标注替代关系。
- AI_CONTRACTS 9.0、词汇记忆提示DESIGN 5.0：核对真实发音，逐音节贴近，禁止为包袱改音；
  ambition/俺必胜、crab/快来剥从正例撤为反例，不合适时换非谐音方法或留空。
  新词/点词/独立提示版本分别升至 6/5/5，schema 不变。
- ROADMAP 2.3 记录本次已做范围；未增加自动音系评分，未自动改写旧提示或验证真实模型效果。

### 2026-09-07（母语阅读与图片搜索提示词优化）

- AI_CONTRACTS 8.0：母语阅读和配图查询提示版本均升至 2，输出结构不变。
  母版加强事实边界、问题推进与理解题质量；替换强调唯一定位、语义保真及整句衔接；
  配图查询加强词义消歧、备用查询差异与无图分支，统一中文说明字段。
- 本次未改调用次数、解析、校验或架构；提示词质量仍需真实生成样本验证。

### 2026-09-07（配图候选从三张放到八张）

- `单词视觉记忆图片DESIGN.md` 1.1：每个词义保留 Top 3 改成 Top 8（§18、§37、§77、§79），
  单次 `count` 从 20 提到 30。原因是硬过滤挺狠，三张换完只剩「重新找」，
  而重搜要再花一次模型加一次搜索；候选是同一次搜索里白拿的。
- §19 的 source dedupe 从「一个站一张」放到「一个站两张」：一刀切会把第二好的图
  换成一张只是来源不重样的图。

### 2026-09-07（单词视觉记忆图片 MVP v1）

- D-071 定下四件事：图片绑 `SenseKey` 不绑词形、Brave 密钥复用现有那一个并放客户端、
  引入 Coil 2.x、图片区放在释义之后而不是词头之上；并记下 `vocabulary_sense_images`
  不进备份的理由（键里嵌着 itemId，恢复时会重映射）。
- ARCHITECTURE 1.5 增加「单词视觉记忆图片」一节与 Coil 依赖，§8 写明 Brave 密钥的两个用途。
- AI_CONTRACTS 7.1 增加「配图检索词调用边界」：`generateVisualSearchPlan` 单独一次调用，
  `visualizable=false` 是合法结论且会落库。
- ROADMAP 2.2 记 M20 的已做范围与未做项（Vision 重排、生成图、敏感词路由、多义辨析题）。
- 新增 L4 专项设计 `单词视觉记忆图片DESIGN.md`（1.0），状态改为部分落地。

### 2026-09-07（中文邪修助记与拼写分组）

- D-069 替代 D-068 双语用法兜底，明确用户偏好的中文声音/字形助记链、留空与旧内容展示过滤。
- AI_CONTRACTS 6.0、记忆提示设计 4.0 同步三条提示版本与 appear 反例，保持 schema=1。
- 拼写设计 2.0、UI_BRIEF 6.0 定义 1～4 组、缺数据整词兜底、取消猜构词、分组用途说明。
- ROADMAP 记录已做范围与未做的历史重写、词典和效果实验。


### 2026-09-06（中文母语者记忆提示重审）

- D-068 替代 D-062 的写法和首屏回想口径，保留草稿/换法与存储边界。
- 词汇记忆提示DESIGN 3.0 补研究来源、适用限制、中文锚点和双语短表达、真正遮挡回想与样本验收。
- AI_CONTRACTS 5.0 同步中文解释、当前词义、问题长度校验与三处提示版本；schema 仍为 1。
- UI_BRIEF 5.0 定义全屏回想与答案核对；ROADMAP 记录本次范围与未验证的长期效果。


### 2026-09-06（手动添加支持中文输入）

- D-067 记录「中文 → 候选 → 完整学习卡」两次调用的分工、本地校验和被否决方案。
- AI_CONTRACTS 4.1 增加候选调用的请求/输出契约；UI_BRIEF 4.0 定义挑目标态的界面口径。

### 2026-09-06（今日页与学习卡草稿态的视觉修正）

- D-066 记录今日页计划行可点性、活跃度收行、最低目标进度条，以及学习卡草稿横幅与底栏动作合并。
- UI_BRIEF 3.0：用户可见文案统一为「添加」，底栏只固定主按钮，草稿出路不再散落在正文末尾。

### 2026-09-06（完整学习卡页面澄清）

- D-065 替代 D-064 的弹窗和查词简版；UI_BRIEF 定义全屏共用学习内容及入库/不入库动作。
- AI_CONTRACTS 4.0：新词请求增加可选 targetTerm / sentenceContext，指定词不匹配拒绝，
  新词提示版本升至 3；普通新词选词逻辑不变。ROADMAP 记录本次修正与未验收项。

### 2026-09-07（母语阅读 MVP）

- 新登记 L4 `母语阅读DESIGN.md`；D-070 记录复用阅读材料表、生成分两次调用、
  检索作为可选增强，以及三处有意偏离（设置页、段内动态难度、回忆不写 FSRS）。
- ARCHITECTURE 增 §7.1（v20 的 `nativeJson`、两步管线、`WebSearchProvider`）；
  AI_CONTRACTS 增 §4.1（两次调用的字段与硬性约定）；ROADMAP 记 M19 的已做与没做。
- M19 补记首轮真机反馈的四处修正（Brave 压缩返回、长按可逆、面板内双击三击、卡片等高）。

### 2026-09-06（记录页生成学习卡）

- D-064、UI_BRIEF 与 ROADMAP 明确记录页「生成 → 预览 → 确认保存」和取消/重试行为。
- AI_CONTRACTS 补充复用调用与完整卡校验；ARCHITECTURE 记录 v19 可选生成信息列及备份兼容。

### 2026-09-06（持续学习首页审视）

- D-063 记录今日页动作顺序、跳过不计完成、收工后隐藏待办；有意调整设计稿的动作顺序。
- ROADMAP 记录已修复范围与热身题量、计划稳定性、跨日刷新和统计覆盖的后续核对项。

只记录**影响 agent 行为**的变更。排版、错别字不记。

### 2026-09-06

- 建立文档层级（L0～L5）、冲突裁决顺序和本索引；`README.md` 的「文档导航」改为指向本文。
- 所有根目录文档加上 frontmatter（`tier` / `status` / `version` / `updated` / `authority`），版本统一从 **1.0 基线**起算——这是本次设定的起点，不是对既往修改次数的追认。`updated` 取该文口径最后一次变更的日期（由 git 历史确认）。
- 新增 `CLAUDE.md`：Claude Code 的会话入口，只转发到 `AGENTS.md` 和本文，规则不复制第二份。
- 新增 `tools/check_docs.py` 与 `Docs` CI 工作流，强制 frontmatter 与 §6 版本表一致。
- `AGENTS.md` 加 §8「文档维护」，把"更新受影响的文档"从一句话细化成可检查的条目。
- `AI_CONTRACTS.md` 修正重复编号：原有两个 §10，第二个改为 §11。
- `单词记忆DESIGN.md`、`英语听力训练模块DESIGN.md`、`语音服务DESIGN.md` 的编号章节从 H1 降为 H2，与其余 14 份对齐——此前它们每章都是 H1，抓不出目录。仅排版，口径未变。
- `README.md` 的「当前状态」从"M0～M6 完成、剩 M7"更新为实际进度（M8～M15.2 已完成，M7 部分完成）。
- `HANDOFF.md` 的交接模板加「文档」一节，开工检查清单改为经 `DOCS.md` 找专项文档。
- `DECISIONS.md` 追加 D-057，记录本次文档体系的决定，含"文件不移动、不改名"的取舍理由。
- `DESIGN.md` 改名为 `UI_BRIEF.md`（D-058），全仓库 20 处引用同步更新，含 6 个 Kotlin 源文件里的注释。
  章节号和屏号不变，只换文件名；老文档和聊天记录里的 "DESIGN.md" 指的就是它。
- `持续学习DESIGN.md` 的一级标题去掉"（DESIGN.md）"，它不是那份界面任务书。

### 2026-09-06（下午）

- 接入高频词优先（`ROADMAP.md` M16、`DECISIONS.md` D-059）：新增 assets 词频表、
  `domain/vocabulary/WordFrequency.kt` 和 `core/data/AssetWordFrequencyIndex`。
- `AI_CONTRACTS.md` **主版本变更**：新增 §3「新词生成请求」，原 §3～§11 顺移为 §4～§12。
  源码注释里指向旧编号的 5 处引用已同步改掉。引用过旧章节号的地方都要重看。
- `ARCHITECTURE.md` §5 增加「词频表」一节：资源位置、分层落点、降级行为。

### 2026-09-06（文案语气）

- 新增可切换的文案语气（`ROADMAP.md` M17、`DECISIONS.md` D-060）：
  `core/designsystem/AppCopy.kt` 收氛围文案，设置页可在正常 / 懒狗之间切换。
- `UI_BRIEF.md` §2 补上语气分档的边界：哪些文案可以变皮、哪些一律照直说。
- `ARCHITECTURE.md` §5 增加「文案语气」一节。
- 懒狗档文案润色（D-061）：`UI_BRIEF.md` §2.1 补充场景化中文互联网口吻、动作清晰和事实准确的写法；覆盖范围与正常档不变。

### 2026-09-06（记忆提示质量与首次换法）

- D-062 替代 D-031 的记忆提示长度与自检标准：英文线索与词义/易错点的联系必须讲通；
  `词汇记忆提示DESIGN.md` 2.0 与 `AI_CONTRACTS.md` 3.0 同步生成、校验、首屏回想口径。
- `UI_BRIEF.md` 补充首次学词和详情页的统一换法交互；新词提示作为草稿随词卡事务入库。
- `ROADMAP.md` M18 记录完成范围与待验收项，并记录整卡重生成的预览替换方案（未实现）。
