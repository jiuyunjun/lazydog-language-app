# 路线图

路线图只描述推荐顺序，不承诺日期。每个里程碑应保持 App 可构建、可验证。

## M0：工程基础

- 创建 Kotlin + Compose Android 工程
- Material 3 主题、导航和基础页面壳
- Room、DataStore 和网络基础设施
- 本地密钥设置及连接测试
- CI 或至少本地 lint/test 命令

验收：无密钥也能启动；基础导航工作；配置不会进入 Git。

## M1：知识库与复习内核

- 单词、语法、学习事件和复习状态模型
- Room schema 与 repository
- 可替换复习调度接口及首版算法
- 知识库列表、筛选和详情
- 调度与迁移单元测试

验收：离线可以创建学习记录、计算下次复习并重启后保留。

## M2：能力测试

- 初始目标和兴趣设置
- 自适应题目流程
- 客观题本地判分
- 初始学习画像与置信度
- 中断恢复

验收：用户完成一次合理长度的测试并得到可解释画像。

## M3：AI 内容与单词/语法学习

- AI provider 抽象
- 结构化输出和契约校验
- 新单词、语法学习页面
- [x] 语法从提示词到 Room、学习卡和记录详情按“结构公式 / 标签 / 简洁用途 / 详细说明 / 正反例 / 提醒”分层
- [x] 分技能能力画像：词汇 / 语法 / 阅读 / 语用 / 表达各存一份连续能力值，生成内容各取各的等级
- [x] 语法从“读讲解”改成“做题”：讲完当场做挖空变形题，到期复习直接出题，复习评分来自客观正确率
- AI 故障、重试和缓存路径

验收：合法内容可以入库；错误返回不会污染数据库。

## M4：渐进式阅读

- 阅读任务选择器
- AI 阅读生成管线
- 已学、复习和新知识比例检查
- 阅读页、点词解释和阅读理解
- 粘贴文本导入

验收：生成内容实际包含指定目标，超出约束时能拒绝或修复。

## M5：朗读

- Azure Speech 配置与连接测试
- 音频播放、录音权限和生命周期
- 朗读识别或发音评估
- 简洁、可理解的反馈
- 能力测试补一道可选的朗读题（DESIGN.md 05 提到、这次对齐设计稿时特意跳过的一项），复用这里的录音与评估管线

验收：真实 Android 设备上可以完成播放—录音—反馈流程。

## M6：完整今日任务

- 根据时长、到期项目和新知识生成计划
- 串联复习、学习、阅读、朗读和测试
- 暂停、恢复、跳过与完成总结
- 学习结果反写知识库和复习计划

验收：10～15 分钟端到端学习闭环稳定工作。

## M7：可靠性与数据保护

- ~~数据导出和导入~~：已提前实现（D-014）。SAF 选定文件夹 + 每日自动备份 + 欢迎页恢复，见 `core/backup/`。还没做：清除所有数据的入口、备份文件的多版本/冲突处理。
- 数据库迁移测试
- 离线和弱网体验
- 无障碍、深色模式和字体缩放检查
- 日志脱敏和性能检查

## M8：情景演练

设计稿见 `懒狗放洋屁 MVP.dc.html` 屏 37～44、PRODUCT.md §6.9、DESIGN.md §5.1。P2 优先级，不进今日必做流程。

- [x] 场景生成：自定义输入 / 按学习目标推荐 / 随机
- [x] 带性格与阻力的角色提示词 + 4～6 条可判定的目标清单
- [x] 对话与目标判定分开调用：判定只返回结构化结果（命中了哪几条、是否沟通失败），不允许顺带纠错或评分
- [x] 四选一默认模式 + 自由输入（含 Azure 语音转文字、卡壳提示）
- [x] 唯一的打断：仅"对方理解反了"时插入行内修正提示，其余问题留到结束后
- [x] 结束总结固定三条最值得改的表达 + 可选的重说关键句 + 留存表达进统一复习计划
- [x] 用 DataStore 记录最近七天的场景 id，生成时去重
- [x] Room 持久化完整会话快照，并从“最近材料”恢复未完成演练
- [x] 长回复选项限高滚动、分阶段友好加载、双方对话朗读
- [x] 全局英文快速双击查词、快速三击讲句，并可加入统一生词本
- [x] 完整句子从单词列表拆分到“表达”，兼容既有 `phrase` 记录并共用复习调度
- [x] 点选回复后对话自动置底；词汇和句子讲解使用 SSE 流式展示
- [x] 表达改为原句/翻译纵排且不重复例句；记录三栏支持左右滑动；粘贴正文非空即可开始

验收：对话中除"对方理解相反"外不出现任何纠错 UI；目标清单每一条都能指向具体轮次；总结固定三条且每条带"你说的 / 改成 / 为什么"；留存的表达能进入复习调度。

实现决定：MVP 不暴露五个独立旋钮，只给“换个更难的对手”整体切档；内部仍把五个难度维度分别传给 AI。情景演练不算进今日必做目标时长。会话以 Room JSON 快照保存，并和阅读材料一起展示在“最近材料”。

仍需真机验收：用实际 AI 配置跑完整场景，验证响应时长、结构化返回稳定性；用 Azure Speech 验证自由输入和重说页的听写。

## M9：摇一摇提问

设计稿见 `懒狗放洋屁 MVP.dc.html` 屏 45～49。P1 优先级，核心闭环已实现。

- [x] `AskContext` 结构化上下文接口，单词 / 语法 / 阅读 / 阅读理解题 / 情景演练五类页面各自注册
- [x] 加速度传感器触发（可调灵敏度、去抖冷却），无传感器或关掉摇一摇时降级为顶栏问号
- [x] 可拖拽提问抽屉：可折叠上下文卡、建议问题、连续追问、SSE 流式回答
- [x] 答案里的新词一键“加进复习”，已存在的词给出提示，不自动入库
- [x] 设置页“随时提问”：摇一摇开关、三档灵敏度、顶栏问号开关
- [ ] 离线时暂存问题、联网后取回（本轮未做：离线直接报错重试）
- [ ] 问答记录落库并挂到词条 / 语法详情页（本轮决定关掉即结束，不落库）
- [ ] 抽屉内语音输入（设计稿的 mic 按钮，本轮只做文字输入）
- [ ] 长按页面空白处作为第三种触发方式
- [ ] 阅读页“当前可视段落”级上下文（现在给的是整篇截断 + 目标词）

验收：五类页面摇出的上下文卡内容正确且可展开查看；关掉抽屉后学习流程原地继续、步骤不重置；追问不需要用户重述对象。

仍需真机验收：摇一摇阈值在走路和公交场景的误触率，三档灵敏度是否够用。

## M10：从输入型改成产出 + 纠错型

起因：真实用户是"词汇量靠积累上来了，变形和语法还在很初级、表达也弱、阅读靠猜词"这一类。
现有流程（认词卡 + 读讲解 + 大意理解题）恰好在喂养这条捷径。M9 的分技能等级和语法做题是第一步，剩下的：

- [x] 错题驱动选题：练习题带形式类别标签，做错入 `drill_mistakes`，语法页显示"你最近老错这些"，留空生成时优先挑能治这些错的语法点
- [x] 每日加一小步产出：两句中译英（今日第三步），判定返回形式错误类别，直接进错题画像
- [x] 阅读理解题换口径：每篇至少一道形式题或指代题，且必须给出正文里的依据句，本地校验
- [x] 单词卡加产出方向：Familiar 以上的词复习时改成中→英打字，本地判分直接映射复习评分
- [ ] 语法点的错误类型统计进能力画像，让"语法等级"随日常表现更新，而不是只靠一次测评
- [ ] 中译英的判定目前每句一次调用，等待偏长；可考虑一次判两句或本地先做明显错误检查

验收：连续用一周后，能看出自己最常错的三类形式问题，并且这三类确实出现在了后面的学习内容里。

体验：AI 等待已做的两件事——语法讲解流式铺内容、讲解回来就后台预取题目。还没做的：今日任务预生成、生成失败的乐观重试。
## M11：听力训练

规格见 `英语听力训练模块DESIGN.md`、PRODUCT.md §6.10。P2 优先级，不进今日必做流程。

- [x] 场景选择（§14 的九个一级场景，各带二级分类进生成提示词）
- [x] 一次生成一轮 10 句：结构化条件（场景 / 二级场景 / 意图 / 语气 / 语体 / 听觉难点）而不是只给 CEFR 等级
- [x] 裸听阶段只有播放键和播放次数，选项藏到「听完了，看选项」之后；四选一先选后确认
- [x] 播放次数计入评分，并实时预告这一题还能拿多少分；答题页可慢速重放
- [x] 四级提示：场景 → 关键词 → 挖空英文 → 完整英文，每级标出代价（后两级由本地按重点表达生成）
- [x] 三个干扰项分属不同误听类型；答错时指出「你听成了什么、为什么」，落到具体的音
- [x] 揭晓页：英文原句、中文、重点表达及其听觉难点、可解释的 Listening Score
- [x] 「再听一次」与「下一句」并排，再听不影响已定的分数
- [x] 一轮总结：总分、四行统计、平均播放次数、最容易绊住人的听觉难点、最常栽的误听类型，以及「再过一遍没一遍听懂的那几句」
- [x] 重点表达一键进入统一复习计划；揭晓页英文支持全局双击查词 / 三击讲句

验收：裸听阶段看不到英文也看不到选项；提示每升一级都要扣分且当场标出代价；四个选项互不重复且三条干扰项类型各不相同；重点表达确实出现在句子里，挖空提示挖的就是它；揭晓页的主按钮是「再听一次」。

明确没做（§25 说的第一版先别做，等核心循环验证过再说）：
- 语音识别、Shadowing 自动评分、多口音、自由输入的 AI 评价、自我判断模式
- 智能混合模式（70% 当前场景 + 20% 弱项复习 + 10% 探索，§14、设计稿屏 50）——
  跨轮次历史现在有了（`listening_attempts`，见 D-052），这一条不再被数据卡住，但仍未实现
- ~~听力能力档案~~：已实现（M14.5 的听力画像，总结页「看看我一直卡在哪一类音上」）。
  周对比（§20、§24）仍未做——要按周聚合，等用一阵子有了足够天数再说
- 一轮训练的**临时状态**仍不落库，中途退出会丢；每题的**结果**已经落库了。
  要做中途恢复得再存"这一轮进行到哪儿"，那是另一件事

仍需真机验收：用实际 AI 配置跑完整一轮，确认十句的生成时长可以接受、结构化返回稳定；确认每题首次播放的等待时间不影响"先听一遍再选"的体验。

## M12：渐进式拼写记忆

规格见 `拼写训练DESIGN.md` 与设计稿 59～64 屏，核心离线闭环已实现。

- [x] Seen → Recognition → Partial Recall → Chunk Recall → Guided Recall → Free Recall → Retained 状态机，允许遗忘降级
- [x] 提示阶梯与 Mastery Credit：答错只增加一级提示，提示越多掌握分越低，且不能跳级看答案。
  2026-09-04 改版：提示从弹窗旁白改成**题面上的骨架逐级显形**（`拼写训练DESIGN.md` §7、D-053）
- [x] 八类拼写错误分类、薄弱片段累计，以及局部/分块题优先挖薄弱位置
- [x] 手滑与遗忘分开：编辑距离 1 且用时 < 2 秒不计连续错误、不降级
- [x] 多维拼写掌握向量、跨日期完整回忆计数和至少七天的 Retained 门槛
- [x] Room v8 持久化拼写进度与逐次尝试；一张卡多次提交只触发一次通用复习调度
- [x] schema v2 备份/恢复拼写记录，并兼容缺少新字段的旧备份
- [x] 「学习」下独立的拼写练习入口（59～61、63 屏），一轮 12 个词，题型按各词阶段自选
- [x] 新词卡的词块拆分（62 屏 S0 接触）
- [x] 拼写能力档案（64 屏）：六维掌握度、高频错误类型、专项训练建议
- [x] 单词复习里熟词的产出卡与拼写练习共用 `SpellingCardView`／`SpellingEngine`，删掉第二套判分
- [x] 拼写队列到期优先，补位的加练不推动通用复习时间

仍需真机验收：在有一定拼写记录之后确认队列排序不总是同一批词、画像页的百分比读起来站得住。

本里程碑不包含设计稿 §20 的扩展项：AI 动态生成干扰项、完整音素—字母训练、FSRS、手写/语音拼写、个人遗忘曲线和 Dashboard。它们需要真实使用数据或新的输入能力，留在 MVP 之后。

## M13：词汇记忆提示

规格见 `词汇记忆提示DESIGN.md`。这是一次**独立的调用**，不是新词生成顺手多写一句：
先判断这个词最值得记什么，再只写那一两种。

- [x] 七类记忆策略（MORPHOLOGY / CONTEXT / ORTHOGRAPHY / PRONUNCIATION / CONTRAST / COLLOCATION / VISUAL_ASSOCIATION），主策略 1 个、次策略 0~1 个
- [x] §6 的结构化输出，字段名与文档一致；示例里的 null 是合法值而不是缺字段
- [x] §10 的质量过滤：站不住的单项删掉，只有核心意思和记忆钩子缺失/超长才整条重来
- [x] §7 的渐进展示：首屏只有核心意思、记忆钩子和策略标签，其余收在「更多记忆提示」里
- [x] 单独生成与重试：一个词一条，「换一条」会把旧钩子和旧策略作为要避开的发出去
- [x] §11 与拼写系统联动：薄弱片段和用户真写错过的形式进提示词
- [x] Room v10 的 `vocabulary_memory_hints` 一词一条，随备份导出，旧备份解码为空
- [x] 生成失败不动已有的那条提示

本里程碑不包含：

- §8 的语义 Progressive Hint 阶梯（意思 → 对比 → 首字母 → 构词 → 答案）。拼写引擎已经有一套五级提示，两套提示合并需要重新定义 Recall Credit 的口径，不是顺手能做的事。
- §13 的个性化权重（`preferred_memory_types`）和 §14 的提示质量指标（7 日回忆率等）。都要先有足够的使用数据，现在算出来的分布只是噪声。
- §12 里按六维掌握度细分的展示口径，目前只用拼写阶段和薄弱片段粗分两档。

仍需真机验收：用实际 AI 配置给几个不同类型的词（`unhappy` / `receive` / `bizarre`）各要一条，确认策略选得不一样，而不是每个词都回到"场景记忆"。

## M14：持续学习

规格见 `持续学习DESIGN.md`。这一份是**产品策略**不是单个功能，37 节里六成已经散落在
M1～M13 里了，所以本里程碑只收"文档要求、代码里还没有"的部分，顺序照文档 §34 的 P0 / P1 / P2。

文档里已经做掉的（不重复记账，位置在括号里）：

- §4 首页单一推荐、§20 的 80/20（`TodayScreen`：一个「继续」按钮，功能入口在「学习」页）
- §5.2 到期优先（`DailyPlanner`：单词永远第一，先还债再上新）
- §9 主动提取的九种取回方式（M10、M12：拼写、中译英、语法做题、熟词产出卡）
- §12 提示阶梯与"用到第几层"的记录（M12 的五级提示 + Mastery Credit）
- §13 Mastery 分级（`KnowledgeStage` 四档 + 拼写 S0～S6）
- §16 能力地图第一层（M3 的分技能能力画像）
- §17／§18 AI Tutor 与场景任务、错误回流复习（M8 情景演练）
- §23 错误系统（M10：`drill_mistakes` + 错题画像 + 留空生成优先治这些错）
- §10.1 的 `MemoryState` 字段（已按 FSRS 预留 stability / difficulty / lapseCount）

### M14.1：进步证据与最低目标（P0 缺口）

对应 §6、§7.1、§14.1、§22。跑在现有的 `learning_events` / `drill_mistakes` /
`spelling_attempts` 上，**不加表、不改 schema**；活跃天数由事件的 `occurredAt` 推。

- [x] `domain/progress/` 纯函数：今日战报（新学几个、回忆几次、想起来几次、翻身了哪几个）
- [x] 活跃度三行（§7.1）：学习旅程 / 最近 30 天活跃 / 当前连续，不做单一 🔥 数字
- [x] 每日最低目标（§6）：五次回忆或走完任一步即达标，然后「再学几分钟」和「今天到这里」并列
- [x] 今日完成卡换成进步证据（§14.1、§22）：不是 +XP，是"上次没想起来、今天想起来了：xxx"
- [x] 文案守则（§6、§26、§29）：「今天到这里」是平等选项不是灰色小字，收工后不再劝学

本节还没做（等真机用一阵子再看要不要）：拼写/听力准确率的日对比（§14.1 的
"76% → 82%"）需要按天聚合的基线，现在只报当天的绝对数；周反馈（§14.2）留到 M14.5
和长期证明一起做。

验收：完成最低目标后能看到一条具体的、指向真实知识点的进步陈述；中断一天不会让页面出现
任何指责性文案。

### M14.2：动态难度（P0 缺口）

对应 §11、§24 的 `recentAccuracy`。地基是 M14.1 的统计层。

- [x] 最近 40 次提取的成功率（`domain/progress/Difficulty.kt`），目标区间 75%～85%
- [x] >90% 时把同一个词从选择题换成生成题；<65% 时首字母直接给（提示阶梯起步抬一级）
- [x] 难度调整可解释：题面上挂一行「最近答得很稳 · 这次问深一点」/「最近有点吃力 · 先给个首字母」

本节还没做：§11 里属于**生成侧**的那几档杠杆（词根、母语解释、降句子复杂度、增加相近词
干扰）——它们要改 AI 提示词并重新校验产出，和这次的"同一批题目换个问法"不是一件事，
留到生成管线下一次动的时候一起做。听力和中译英暂时不受偏置影响，同样是生成侧的事。

### M14.3：FSRS（P0 缺口）

对应 §10。`MemoryState` 的字段已经预留，缺的是算法本体。

- [x] 用 FSRS 替换 `SimpleIntervalScheduler`（`ReviewScheduler` 接口不变，旧实现已删）
- [x] 存量数据：**不迁移**，下一次复习起自然按新算法走（用户 2026-09-04 决定，见 D-047）
- [x] §10.3 的统一调度：单词、表达（整句）、语法本来就共用 `knowledge_items` 和同一个
  调度器，这一条已经成立。听力块和发音模式还不是知识项，等它们真的需要复习时再说。

本节没做：个人参数优化（FSRS 的 optimizer）。它要成百上千条复习记录才有意义，
现在跑只会拟合噪声；官方默认参数已经比固定倍数好得多。期望保留率也没有做成设置项——
先用 0.9 跑一阵，有实感之后再决定要不要暴露。

### M14.4：中断回归与宽容（P1）

对应 §7.2、§25、§26。

- [x] Recovery Flow：断三天以上回来只排一步、文案换成"不用补以前的"，**不提积压数量**
- [x] Streak 宽容：每周自动允许休一天（用户 2026-09-04 决定，不做冻结券，见 D-048）
- [x] 疲劳信号（§25）：今天做够多且最近连着错 → 今天只排一步短复习

本节没做：§25 里"提示使用率上升""跳过增多""退出次数"这三个信号（跳过和退出没有在记），
以及"反应时间突然增加"（需要个人基线，慢多少算慢得先知道这人平时多快）。
§7.2 的旅行 / 生病模式也没做——每周一休已经覆盖了绝大多数中断，
再加两个需要手动切换的模式只是让人多管一件事。

### M14.5：进步的可证明性（P1）

对应 §14.3、§15、§16 第二层。

- [x] 长期证明（§14.3）："N 天前你还会在这里出错，当时写的是 recieve"——指到具体那一次作答
- [x] Proof of Progress 挑战：用 2～4 周前学的四个词组一句，先听、再认关键词、最后揭晓
- [x] 能力地图第二层：听力细分到连读 / 数字 / 弱读各自的正确率（听力画像）

长期证明用的是 `spelling_attempts`：它存了当时**真的写成什么**，所以证据能落到
"你把 receive 写成 recieve"这种程度，而不是泛泛一句"拼写进步了"。要求最近一次是
**无提示**写对的——提示答对不足以证明会了。

进步挑战在「学习」页的「还记得吗」入口。凑不够 4 个两到四周前学的词就直接说"再过些日子来"，
不用刚学的词凑数——凑出来的证明证明不了任何事。整关**没有分数、没有计时、没有连击**，
播放次数也不计分：这一关证明的是"听懂了"，不是"一遍就听懂了"。

**§15 的"复述"没做**：它和现有的朗读评估是同一件事（`assessReading`），
再实现一遍只会多一处录音权限流程。听懂 + 认出关键词已经足够支撑"以前听不懂，现在听懂了"。

**能力地图第二层（§16）**已经做了：听力作答落库（Room v16 的 `listening_attempts`，见 D-052），
听力画像按听觉难点分项给正确率，最弱的排最前面。入口在听力总结页。
拼写那一维本来就有——M12 的六维掌握度就是它的第二层。

### M14.6：兴趣个性化（P1）

对应 §19。onboarding 已经收了学习目标，缺的是兴趣标签和它对例句的影响。

- [x] 例句提示词带上用户勾的兴趣：新词生成和双击查词的讲解都会优先落在他自己的领域

兴趣标签本来就有（引导页的 `TopicCatalog.starter`），原来只影响**选哪些词**和阅读/听力的
取材，不影响**例句写成什么样**——所以词是贴着兴趣挑的，句子还是"Tom bought an apple"。
现在 `exampleSentenceRules` 收兴趣，并且明说这是偏好不是硬要求：词和领域挨不上时
写它最自然的场景，硬塞出来的别扭句子比无关句子更糟。

### 明确不做

- §27 / §28 的 P2 游戏化里除 Achievement 外的全部：好友、联赛、头像、商店、虚拟货币。
  文档自己把它们排在最后，而这是个人自用 App，社交和货币都没有对象。
- §30～§33 的埋点、指标体系和 A/B 实验：单用户跑不出统计显著性，
  埋点只会变成没人看的数据。进步证据自己就是给用户看的，不需要另一套后台指标。
- §2 的 North Star（Weekly Proven Progress）作为**指标**不做，但它背后的想法
  —— 用能力增长而不是签到驱动 —— 就是 M14.1 要落到界面上的东西。

## M15：值得读的阅读材料

规格见 `引人入胜的阅读材料DESIGN.md`。它要解决的不是"生成符合 CEFR 的文章"，
而是"用户主动想点开、愿意读完、读完觉得有收获"。顺序照文档自己的 §24（MVP v1 / v2 / v3）。

文档里已经做掉的（位置在括号里）：

- §16 的题目设计：一次不弹十道题，3～4 道且**至少一道形式题或指代题**，
  还要求给出正文里的依据句并本地校验（M10）
- §17.2 的点击查词：全局双击查词、三击讲句，不默认高亮目标词（M8「全局英文交互」）
- §7.1 的目标词轻量注入：复习词必须自然出现、新词有上限、引文必须来自正文（M4）
- §22 的 SRS 融合方向：到期词进提示词，但正文自然度优先——不做"20 个词硬编成文章"
- §5 的兴趣输入：`TopicCatalog`，用户勾过的兴趣排在最前面

### M15.1：让文章值得读（P0）

对应 §3、§4、§6、§8、§9、§20。全在生成层加一点 UI，不碰推荐系统。

- [x] 10 种 article archetype（§6），按最近用过的轮换（`domain/generation/ReadingArchetype.kt`）
- [x] Hook → Curiosity Gap → Progressive Discovery → Payoff → Takeaway 的结构约束（§3）
- [x] `reader_payoff`（§4）：本地硬校验（非空、不超长、不是标题复述、不是套话），
  读完显示「值得记住的一件事」
- [x] 标题与开头的黑名单（§9、§3.1）：标题党和 "In today's world" 一律拒绝，不是警告
- [x] 节奏（§8）：目标词数从 160 提到 300，并要求 5～9 段、每段 30～80 词
- [x] Anti-Repetition（§20）：最近 12 篇的标题和写法进提示词

Room v17 给 `reading_materials` 加了 `readerPayoff` 和 `archetype` 两列（都带默认值，
自动迁移）；备份格式升到 5，旧备份缺这两项解成空串。

### M15.2：反馈闭环与 Feed（P1）

对应 §17.1、§18、§14、§15。

- [ ] Feed Card（§17.1）：标题 + teaser + 阅读时间 + 类别，CEFR 和词数退到次要位置
- [ ] 读完的行为记录（§18）：completed / liked / saved，先只记这三个能明确表达意图的
- [ ] Interest Critic 与无聊检测（§14、§15）：低于阈值自动重写一次

### M15.3：主题引擎与推荐（P2）

对应 §5、§10、§19、§21。

- [ ] Topic Candidate → Ranker（§10.1、§10.2）：一次出 10～30 个候选再排序，而不是用户手挑一个词
- [ ] 探索/利用混合（§5.1、§21）：50% 高置信兴趣 + 20% 相邻 + 20% 普适 + 10% 惊喜

### 明确不做

- §11 的 Fact Pack 与来源核验、§19 的 embedding 推荐、§25 的 bandit：
  它们要么需要联网检索，要么需要成规模的行为数据。个人自用 App 两样都没有。
- §18 的完整行为埋点（impression / scrollDepth / openDelayMs / abandonedParagraph）：
  单用户跑不出统计意义，只会变成没人看的数据。只记"完成/喜欢/保存"这三个明确的意图信号。
- §25 的 KPI 体系：同上，不做后台指标；「值得记住的一件事」本身就是给用户看的反馈。

## 已知限制

- ~~**语速设置对例句和短文无效**~~：已解决（D-044）。Dragon HD 不支持 SSML 的 `<prosody>`，
  所以现在**只要这次要变速就换成同一个人的标准 Neural**，语速真的生效；正常语速的例句和
  短文仍然走 HD 的自然度。代价是变速时音色会变，设置页已经写明。
  听力页的「慢速重放」原来是个假按钮，也是这次一起修好的。
- **朗读全文没有逐句高亮**：同一原因（HD 音色不支持 `<bookmark>`），已评估并撤回，见 D-040。

## MVP 之后

- URL 正文提取
- Search API 与真实文章推荐
- 更完整的 FSRS 实现和参数优化
- 更丰富的写作与听力训练
- 根据长期数据优化阅读难度比例
- 可选加密备份或用户自管同步
