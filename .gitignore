# Android Studio / IntelliJ
.idea/
*.iml

# VS Code Local History extension
.history/

# Gradle
.gradle/
build/
**/build/
local.properties

# Signing and secrets
*.jks
*.keystore
secrets.properties
.env
.env.*

# Local application data
*.db
*.db-shm
*.db-wal
*.wav
*.mp3

# OS
.DS_Store
Thumbs.db

# Local hardcoded credentials (see LocalEnv.kt.example)
LocalEnv.kt

# 设计画布的发布产物（约 2MB，由 design/*.dc.html + canvas.json 重新生成）
design/lazydog-new-screens.html
design/title.txt

# 母语阅读设计画布的发布产物（约 2MB，由 design/native-reading/*.dc.html 重新生成）
design/native-reading/native-bridge-reading.html
design/native-reading/.title.txt

# 单词视觉记忆图片设计画布的发布产物（同上，由 design/visual-memory/*.dc.html 重新生成）
design/visual-memory/vocabulary-visual-memory.html
design/visual-memory/.title.tmp
