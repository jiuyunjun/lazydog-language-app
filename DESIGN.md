# Claude Design 任务书

## 1. 你的角色

你是“懒狗放洋屁”的产品设计 agent。请基于本文件和 `PRODUCT.md`，设计一套可以由 Android Jetpack Compose + Material 3 直接实现的 MVP 界面与交互。

你的任务是降低学习启动成本并让完整学习链路清晰，不是创造品牌展示稿。不得自行增加社交、排行榜、宠物养成、复杂动画或其他未列入 MVP 的功能。

## 2. 产品气质

关键词：极简、轻松、安静、可信、略带幽默。

“懒狗”体现在低压力文案和对用户节奏的尊重，不体现在幼稚卡通、过度装饰或故意粗糙。界面首先是一款可靠的学习工具。

文案示例：

- “今天少学一点，也算学了。”
- “这词又忘了？正常，再见它一次。”
- “放洋屁时间到了。”

幽默文案应少量出现，关键设置、错误和隐私说明必须直接准确。

## 3. 视觉和实现约束

- 使用 Material 3 默认组件和系统字体。
- 同时支持浅色、深色和系统主题。
- 尽量使用默认颜色体系，只定义少量语义色。
- 不依赖定制插画、复杂图标包、玻璃拟态或重动画。
- 保证系统字体放大、TalkBack 语义和足够触摸区域。
- 一级底部导航最多四项：今天、学习、记录、设置。
- 每个页面只有一个视觉明确的主操作。
- 所有核心流程必须可单手完成。

## 4. 信息架构

### 4.1 今天

- 今日任务摘要
- 预计用时
- 待复习数量
- 当前连续任务进度，但不要制造打卡压力
- 主按钮：“开始今天的学习”或“继续学习”
- 次按钮：“自由学习”

### 4.2 学习

- 单词
- 语法
- 阅读
- 朗读
- 情景演练（第五个入口，也可作为今日流程的可选加餐）
- 已保存或最近使用的学习内容

### 4.3 记录

- 已学单词库
- 已学语法库
- 按掌握状态、到期时间和最近错误筛选
- 简洁的近期学习回顾

### 4.4 设置

- 每日目标时长和新知识数量
- 学习目标与兴趣
- AI 服务和密钥
- Azure Speech 配置
- 主题、音频和录音保留选项
- 数据导出、导入和清除

## 5. 必须设计的流程

请覆盖以下状态，而不只画理想主页面：

1. 首次启动与服务配置
2. 自适应能力测试
3. 测试结果与学习画像确认
4. 今日任务预览
5. 单词学习
6. 语法学习
7. 渐进式阅读与点词解释
8. 朗读、录音授权和反馈
9. 小测试与四档自评
10. 今日完成总结
11. 单词/语法知识库和详情
12. 粘贴文本生成学习材料
13. 加载、空状态、离线、AI 超时、格式错误和重试
14. 中途退出和恢复任务
15. 情景演练：选场景（自定义 / 按目标推荐 / 随机）
16. 情景演练：开演前简报（处境、对手性格、目标清单、难度旋钮）
17. 情景演练：对话（四选一默认模式 + 自己输入，支持语音转文字和半句提示）
18. 情景演练：唯一的打断——仅当对方把意思理解反、沟通无法继续时插入一条修正提示
19. 情景演练：任务总结（目标达成情况 + 固定三条最值得改的表达，各带"你说的 / 改成 / 为什么"）
20. 情景演练：重说关键句（只重演做错的那 2～3 轮，可跳过）与收尾（留存表达进 FSRS 复习）

### 5.1 情景演练的专项要求

- 一次演练 = 一个有目标的场景 + 一个有性格和阻力的对手 + 一份 4～6 条可判定的完成清单（如：说明问题 / 提供订单信息 / 提出要求 / 回应拒绝 / 达成方案）。
- 对话中不做语法/用词纠错。唯一允许的打断是"对方理解反了、对话没法继续"这一种，用行内提示条呈现（不是弹窗），给出对方听成了什么、一个改写建议，用户可以"就这么说"或"我自己重说"。语法生硬、用词不地道等问题一律留到结束后一次性给。
- 难度不是靠换更难的单词，而是靠五个旋钮：信息量、对方合作度、追问强度、是否需要礼貌拒绝、是否埋一个误解。词汇难度跟随用户已测出的等级独立设置，不跟这五个旋钮混在一起。
- 对话生成和目标判定是两次独立的模型调用：判定只返回结构化结果（命中了哪几条子目标、是否发生沟通失败），不允许在对话里顺带纠错或评分。
- 结束后固定给三条最值得改的表达（不多给），可选择重说关键那几句（只重演 2～3 轮），最后收尾时把留下的表达送入 FSRS 复习计划。
- 同一个场景一周内不重复出现。
- 对话回复选项区域最高 300 dp；内容更长时区域内滚动，避免把输入与主要动作挤出屏幕。
- 生成简报、等待角色回复和恢复会话时使用带阶段说明的加载状态，不只显示孤立的进度圈。
- 情景会话进入学习页“最近材料”；中途退出时保存对话、目标、草稿和阶段，下次从原处继续。
- 对话双方的英文气泡均提供朗读按钮。
- 用户发送或点选回复后，对话列表自动滚到最新一轮和等待状态。

## 6. 关键交互要求

- 今日学习应表现为一条连续流程，用户不必频繁返回首页选功能。
- 学习过程中始终能看到当前步骤和剩余量，但不使用制造焦虑的倒计时。
- 学习内容中的英文统一支持快速双击查词、快速三击讲句；讲解使用底部抽屉并流式展示，不遮断当前上下文。词汇可直接放进生词本，完整句子进入独立“表达”记录，不混入单词列表。
- “记录”的单词、语法、表达三个 Tab 同时支持点按和左右滑动切换。表达列表采用英文原句在上、中文翻译在下的纵向排列；详情不重复显示例句，只保留原句、翻译和原句朗读。
- 语法列表采用“英文结构公式在上、一句话中文用途在下”，不得把长篇说明塞进副标题。学习卡先展示结构公式、中文标签和一句话用途，再按“怎么用 / 正确例句 / 易错例 / 易混提醒”逐层展开；记录详情保留同样的信息层级。
- 粘贴阅读只要正文含有非空白字符即可开始；标题继续允许留空并自动取正文前几个词。
- 正确/错误反馈应可被颜色之外的文字或图标理解。
- 朗读权限只在用户进入朗读并执行录音时请求。
- API 密钥输入框默认遮挡，提供测试连接与明确的本地保存说明。
- 错误页面必须保留已经完成的步骤，并提供重试、跳过或稍后继续。

## 7. 交付物

设计输出必须包含：

1. 页面清单与导航关系
2. 核心学习流程图
3. 各页面低保真线框或明确布局说明
4. 每个页面的组件、状态和主要交互
5. Material 3 组件映射
6. 浅色/深色主题令牌建议
7. 关键中文文案
8. 加载、空状态、错误、权限和离线状态
9. 无障碍检查清单
10. 交付给编码 agent 的实现优先级与验收要点

如果生成高保真稿，请同时提供不依赖图片也能实现的规格说明。不要仅交付截图。

## 8. 需要明确标注的假设

尚未确认的信息不得自行当作产品决定。请在输出末尾列出：

- 采用的假设
- 仍需用户决定的问题
- 可能影响开发成本的设计
