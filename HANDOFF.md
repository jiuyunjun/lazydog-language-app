---
doc: "HANDOFF.md"
tier: "L1 工作规则"
status: "生效"
version: "1.1"
updated: "2026-09-06"
authority: "agent 与人之间的交接格式和阻塞处理方式"
index: "DOCS.md"
maintenance: "改本文须同步 DOCS.md 的版本表，校验命令 python tools/check_docs.py"
---

# Agent 交接规范

## 1. 目的

本文件用于 ChatGPT、Claude Design、Claude Code、其他编码 agent 与人工之间的任务交接。交接必须让下一位参与者不依赖聊天记录也能继续工作。

## 2. 开始任务前

接手者必须：

1. 阅读 `AGENTS.md`。
2. 阅读 `DOCS.md`，据它找出本次任务相关的专项文档——不要凭文件名猜，也不要一次全读。
3. 检查 `git status --short --branch` 和最近提交。
4. 区分已有未提交修改与本次任务范围。
5. 明确验收条件和不做事项。

注意：`DOCS.md` §4 有两条规则专门用来防止交接时传错事实——专项设计写了不等于已实现，判断现状要看 `ROADMAP.md` 的勾选状态和 `DECISIONS.md`。

## 3. 标准交接模板

```markdown
## 任务

一句话说明目标。

## 状态

完成 / 部分完成 / 阻塞

## 已完成

- 具体行为和用户可见结果

## 修改文件

- `path/to/file`: 修改原因

## 关键决定

- 决定、原因和被否决方案

## 验证

- 执行的命令
- 结果
- 未执行的验证及原因

## 文档

- 更新了哪几份、为什么（对照 `AGENTS.md` §8 的表）
- 版本号和 `DOCS.md` 版本表是否已同步
- `python tools/check_docs.py` 的结果

## Git

- 分支
- 提交哈希和提交信息
- 工作区是否干净

## 风险与已知问题

- 仍存在的问题及影响

## 下一步

1. 下一项最小可执行任务
```

## 4. 设计交给编码

Claude Design 的交接除标准模板外，还必须包含：

- 页面和导航清单
- 组件到 Material 3 的映射
- 所有非理想状态
- 主题和间距令牌
- 可访问性要求
- 无法从图稿判断的交互说明
- 哪些内容是已确认决定，哪些只是建议

设计截图不能代替文字规格。

## 5. 编码交给设计或审查

编码 agent 必须说明：

- 哪些设计已经实现
- 哪些因平台约束调整以及原因
- 可运行入口和测试设备配置
- 截图或录屏对应的版本
- 尚未覆盖的状态

## 6. 阻塞处理

遇到密钥、外部服务、用户选择或设备能力阻塞时：

- 先完成不依赖阻塞项的安全工作。
- 写明已经验证过的事实，不只写“不可用”。
- 提供一个推荐选项和替代方案。
- 不伪造外部服务结果，不把未验证工作写成完成。
