---
doc: "AGENTS.md"
tier: "L1 工作规则"
status: "生效"
version: "1.1"
updated: "2026-09-06"
authority: "所有 agent 的强制工程规则、Git 方式、实现流程与完成定义"
index: "DOCS.md"
maintenance: "改本文须同步 DOCS.md 的版本表，校验命令 python tools/check_docs.py"
---

# AGENTS.md

本文件适用于仓库根目录及全部子目录。所有参与本项目的代码、设计和审查 agent 在修改前必须阅读本文件，以及与任务有关的专项文档。

**去哪儿找哪份文档，见 [`DOCS.md`](DOCS.md)**：那里有完整清单、每份文档管什么、什么时候读、冲突了听谁的、现在是哪一版。开工前只需要读本文和 `DOCS.md` 两份，其余按需加载。

## 1. 项目目标

“懒狗放洋屁”是一款供中文母语者私人使用的英语学习 Android App。核心目标不是堆积课程，而是让用户以尽可能低的启动成本完成：

```text
学新知识 → 在语境中再次遇见 → 朗读 → 测试 → 按遗忘规律复习
```

产品范围和验收标准以 `PRODUCT.md` 为准；界面任务以 `DESIGN.md` 为准；技术实现以 `ARCHITECTURE.md` 和 `AI_CONTRACTS.md` 为准。完整的冲突裁决顺序见 `DOCS.md` §4。

## 2. 强制工程规则

- 使用 Kotlin 开发 Android 原生应用。
- 新界面优先使用 Jetpack Compose、Material 3 和 Android 默认交互。
- 除非文档明确变更，不引入自建服务端、账户系统或云同步。
- 本地持久化数据不得依赖 AI 返回结果作为唯一事实来源。
- AI 输出必须结构化、经过解析和业务校验后才能入库。
- 所有文本文件均使用 UTF-8；不得使用系统默认编码写文件。
- 不提交真实 API 密钥、个人数据、录音或本地数据库。
- 不为了“以后可能需要”提前引入复杂抽象或大型依赖。
- **架构边界见 `ARCHITECTURE.md` §0，那是硬约束。普通任务的架构变更预算为 0**：
  可以改实现、给已有抽象加实现、在同一层加模块；新增层、新框架、改依赖方向、造平行抽象
  都必须先停下来提架构变更请求。
- 不擅自扩大 MVP 范围。发现新需求时，先记录到 `ROADMAP.md`，不要顺手实现。

## 3. Git 工作方式

- 开始任务前运行 `git status --short`，识别并保护用户或其他 agent 的未提交修改。
- 一个提交只表达一个完整、可解释的意图。
- 每个完成且通过验证的任务必须提交 Git；不要把多个无关任务积压到同一提交。
- 提交信息使用英文 Conventional Commits，例如：
  - `docs: define MVP learning loop`
  - `feat: add vocabulary review queue`
  - `fix: reject malformed reading payloads`
- 禁止使用 `git reset --hard`、强制推送或覆盖他人修改。
- 未通过验证的工作不得标记为完成；如必须中断，在 `HANDOFF.md` 格式下清楚交接，不制造“半完成”提交。

## 4. 实现流程

1. 阅读相关文档和已有实现。
2. 明确本次任务的范围、验收条件及不做事项。
3. 做影响分析（`ARCHITECTURE.md` §0.6）：这件事归谁管、预计改哪些文件、是否需要新依赖或
   架构变更。和现有架构冲突时先说明冲突，不要边实现边绕开。
4. 实现最小但完整的改动。优先扩展已有抽象，不要另起一套（§0.4）。
5. 运行与改动风险相称的格式化、静态检查和测试。
6. 做一次架构漂移自查（`ARCHITECTURE.md` §0.6 的 8 条），有漂移就明确报出来。
7. 检查 `git diff`，确认没有密钥、生成物或无关修改。
8. 按 §8 更新受影响的文档，并跑 `python tools/check_docs.py`。
9. 创建 Git 提交，并在交接中给出提交哈希和验证结果。

## 5. UI 与文案

- UI 追求安静、直接、单手可操作，不追求视觉炫技。
- 能使用 Material 3 默认组件时不自制组件。
- 每个页面只保留一个明确的主要动作。
- 底部一级导航最多四项。
- 中文负责解释和引导，英文是主要学习对象。
- 产品文案可以轻松、有一点“懒狗”性格，但不得羞辱、催促或制造学习焦虑。
- 无障碍语义、字体缩放、深色模式和触摸目标大小属于基础质量，不是后续装饰。

## 6. 数据与 AI

- 单词和语法的掌握状态、复习时间、测试结果由本地领域逻辑维护。
- AI 可以提出建议，不能直接决定用户“已经掌握”。
- 生成内容必须保留生成参数、模型标识、时间和校验状态，便于复现与排错。
- 对网络失败、限流、超时、格式错误和内容不合格提供可恢复路径。
- 密钥可以由用户保存在 App 本地，但不得硬编码进源码或提交到 Git。

## 7. 完成定义

一个任务只有同时满足以下条件才算完成：

- 功能或文档符合验收要求。
- 相关测试或人工验证已经执行并记录。
- 没有引入已知的高优先级回归。
- 文档与实际行为一致。
- 改动已经形成 Git 提交。

## 8. 文档维护

文档层级、每份文档的作用和冲突裁决顺序见 `DOCS.md`。这里只写**你干完活之后必须做什么**。

- 按下表更新文档，不要把"文档回头再补"留给下一个人：

  | 你干了什么 | 必须更新 |
  | --- | --- |
  | 完成里程碑中的一条 | `ROADMAP.md` 勾选，并写清这一条**没做**的部分 |
  | 做了一个文档没写过的选择 | `DECISIONS.md` 追加 D-0xx（含原因和被否决方案） |
  | 有意偏离某份专项设计 | `DECISIONS.md` 记下偏离和原因，不要默默改专项设计装作一直如此 |
  | 改了 AI 的入参、出参或提示词口径 | `AI_CONTRACTS.md` |
  | 加表、加层、改依赖方向 | `ARCHITECTURE.md`，且必须先走架构变更请求 |
  | 发现新需求但这次不做 | `ROADMAP.md`，不要顺手实现 |

- **改了任何文档的口径，三处一起改**：该文 frontmatter 的 `version` / `updated`、`DOCS.md`
  §6 版本表、`DOCS.md` §7 变更日志。版本号规则见 `DOCS.md` §5.2：口径变更进主版本，
  补充澄清进次版本；纯排版和错别字两个都不动。
- 新增一份根目录文档时，必须同时登记进 `DOCS.md` §3 和 §6，否则校验不过。
- 提交前跑 `python tools/check_docs.py`，CI 的 `Docs` 工作流会跑同一条命令。
- 推翻旧决定时保留旧记录并注明替代关系，不要删除历史。
- 专项设计（L4）描述的是目标形态。**不得因为文档写了某功能就断言它已实现**；判断现状看
  `ROADMAP.md` 的勾选状态、`DECISIONS.md` 和代码本身。
