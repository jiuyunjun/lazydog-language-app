# 决策记录

这里记录已经确认、会影响后续实现的决定。新决定追加记录；如果推翻旧决定，应保留旧记录并注明替代关系。

## D-001：产品形态

- 状态：已确认
- 决定：私人使用、中文母语者学习英语的 Android App。
- 原因：保持目标和首版范围清晰。

## D-002：主学习模式

- 状态：已确认
- 决定：同时提供 AI 今日任务和自由学习，但以今日任务为主。
- 原因：减少规划成本，同时保留用户主动性。

## D-003：首版深度优先

- 状态：已确认
- 决定：接受功能朴素，优先跑通单词、语法、阅读、朗读、测试和复习的完整链路。
- 原因：先验证闭环价值，避免多个孤立功能。

## D-004：初始水平

- 状态：已确认
- 决定：不要求用户自报等级，通过自适应测试建立初始画像，并在使用中持续修正。

## D-005：记忆与知识库

- 状态：已确认
- 决定：维护已学单词和语法知识库，按照遗忘规律安排复习；AI 不直接决定掌握状态。

## D-006：渐进式阅读

- 状态：已确认
- 决定：阅读以已掌握内容为主，混入到期复习内容和少量新知识。初始比例为产品假设，需通过实际使用调整。

## D-007：Search API

- 状态：已确认
- 决定：MVP 不接入 Search API，但阅读来源接口应允许以后加入。

## D-008：技术平台

- 状态：已确认
- 决定：Android 原生 Kotlin；优先 Jetpack Compose 和 Material 3 默认 UI；无自建服务端。

## D-009：密钥处理

- 状态：已确认
- 决定：接受私人 App 在客户端保存在线服务密钥；通过设置页保存，不将真实密钥硬编码或提交到 Git。

## D-010：每日默认量

- 状态：暂定，待实测
- 决定：10～15 分钟、最多 5 个新单词、最多 1 个新语法点、1 篇短文。复习积压时优先复习，不强制加入新内容。

## D-011：工程协作

- 状态：已确认
- 决定：全部文本文件使用 UTF-8；每个完成任务形成独立 Git 提交；ChatGPT/Claude 使用标准 handoff。

## D-012：本地写死服务配置（替代 D-009 的“设置页保存”部分）

- 状态：已确认（2026-08-04，用户决定）
- 决定：私人使用阶段，AI 与 Azure Speech 配置写死在 `core/config/LocalEnv.kt`，该文件被 `.gitignore` 忽略，仓库只保留 `LocalEnv.kt.example` 模板。onboarding 不再要求用户填写服务配置；DataStore 中存过的值优先于 LocalEnv，后续开放设置页手动配置时无需改读取逻辑。
- 原因：单人使用，降低每次安装的配置成本。“不将密钥提交到 Git”的底线不变。

## D-013：朗读（M5）提前到 M1 之后实现

- 状态：已确认（2026-08-04，用户决定）
- 决定：在 M2（能力测试）和 M3（AI 内容）之前先接入 Azure Speech 朗读：TTS 示范、录音发音评估和反馈。朗读结果反写复习计划仍留在 M6。
- 原因：Speech 密钥已配好且可独立验证；朗读闭环不依赖 AI provider。

## D-014：数据持久化用 SAF 选定文件夹 + 自动导出，不用 MANAGE_EXTERNAL_STORAGE

- 状态：已确认（2026-08-05，用户决定）
- 决定：不把 Room 数据库直接放进公共存储，也不申请"所有文件访问权限"。改为让用户在设置里通过标准的 SAF 文件夹选择器（`ACTION_OPEN_DOCUMENT_TREE`）授权一个外部文件夹，App 每天自动把知识库和学习偏好导出成一份 JSON 写进去；首次启动的欢迎页也能选同一个文件夹来恢复。
- 原因：MANAGE_EXTERNAL_STORAGE 需要用户去系统设置手动开一个特殊权限开关，且以后如果上架 Google Play 这个权限会被卡审核；SAF 不需要敏感权限，兼容性更好。代价是 App 卸载重装后，Android 的 URI 授权会失效（跟着应用签名+包名走），文件本身还在文件夹里，但用户需要在欢迎页重新选一次同一个文件夹才能恢复，不是完全零操作。
- 被否决方案：把 Room 数据库文件直接放在 `Environment.getExternalStorageDirectory()` 下的固定路径，靠 MANAGE_EXTERNAL_STORAGE 全权限读写——零操作但权限过重，个人分发虽然无所谓，但不符合"够用就好、不为将来可能的上架预留"的原则。

## D-015：能力测试和新词生成对齐《CEFR 英语能力评测与个性化学习系统设计.md》

- 状态：已确认（2026-08-05，用户决定）
- 决定：能力测试引擎换成该文档的规则——连续能力值加入 Pre-A1（0.0）锚点；客观题梯度覆盖约束从 4 类扩到 5 类（新增"纠错或短答"，允许部分正确，本地按编辑距离相似度打分，不用 AI 判分）；答对但用时异常（疑似猜测/明显超时）只算弱证据、涨分打对折。新词生成从"AI 随便挑词"改成按词形+词性+具体词义生成，每个词附 1~2 个真实高价值搭配，不再是孤立单词表。
- 原因：用户直接要求按新文档对齐。
- 明确没做的（文档 §8 自己也认为不受控生成会牺牲可靠性，个人开发阶段先不追）：审核过的分级词库 + IRT/Rasch 统计校准（题目和词义的 CEFR 等级目前仍是 AI 实时判断，不是"经过标定"）；词义级的 6 维掌握度模型（识别/语境/回忆/使用/搭配/发音分别打分）——现在仍是单一的 FSRS 式调度状态；候选词的完整优先级公式（频率×相关性×等级适配×迁移价值）——现在只用"已学过的词排除"做缺口代理，没有真实频率/语域数据支撑更精细的排序。这些留给以后有真实使用数据时再实现。

## D-016：情景演练难度与今日任务

- 状态：已确认（MVP 实现口径）
- 决定：用户只使用“换个更难的对手”整体切档，不直接调整五个独立旋钮；内部继续分别维护信息量、合作度、追问强度、礼貌拒绝和误解。情景演练是学习页第五个入口，不计入今日必做目标时长。
- 原因：保持开演前页面简单，同时不牺牲生成约束的可解释性。

## D-017：情景会话快照与全局英文手势

- 状态：已确认（2026-08-05，用户要求）
- 决定：情景演练用 Room 保存 JSON 会话快照，进入统一“最近材料”并支持中断恢复。主要英文学习内容统一使用快速双击查词、快速三击讲句；讲解复用阅读功能，词汇可进入统一生词本。
- 原因：情景演练属于可反复使用的学习材料，不能因退出而丢失；统一手势可避免每个页面重复设计一套查词入口。

## D-018：完整句子归入“表达”而不是“单词”

- 状态：已确认（2026-08-05，用户反馈）
- 决定：知识库显示为单词、语法、表达三类。表达继续复用现有知识项和复习调度，在 `vocabulary_details.pos` 中标记为 `expression`，并兼容此前的 `phrase`；不为它新建相同结构的数据表。句子讲解可把整句摘入表达。
- 原因：完整句子与可复用短语有独立浏览价值，但底层记忆状态、中文释义和例句结构与现有记录一致，当前无需承担数据库迁移和重复调度逻辑的成本。

## D-019：语法以结构公式为主键并分层保存说明

- 状态：已确认（2026-08-05，用户反馈）
- 决定：语法主标题固定为纯英文结构公式；中文标签、一句话用途、完整讲解、正例翻译、错例说明和易混提醒分别生成、校验并保存。列表只显示结构公式与一句话用途，学习卡和详情逐层展开其余内容。
- 兼容：Room v5 保留旧 `name` 列作为唯一键，新记录令其等于 `patternEn`；旧记录通过展示回退拆分混合标题，不要求清库。
- 原因：标题用于识别“形式”，一句话用途用于快速回忆，详细讲解用于理解；混用字段会让 AI 输出、去重、卡片布局和复习提示同时失真。

## D-020：听力训练只做核心循环，不落库

- 状态：已确认（2026-08-31，按 `英语听力训练模块DESIGN.md` §25 的 MVP 范围实现）
- 决定：听力训练做成「学习」下的第六个入口，一轮 10 句，只实现「只听声音 → 猜意思 → 揭晓 → 再听一次」这条循环。一轮的状态活在页面里，不进 Room，中途退出会明确提示这一轮会丢。
- 原因：文档 §25 自己把第一版限定在验证这个循环上；§14 的智能混合模式、§20 的听力能力画像、§23 的最强/最弱场景和 §24 的周对比全都需要跨轮次历史，没有历史就只能编数字。先不建表，等这个循环被真机验证过、确实要留历史时，再一次把表和统计一起做，比现在先加一张用不上的表划算。
- 明确没做：语音识别、Shadowing 自动评分、多口音、自由输入的 AI 评价、自我判断模式。
- 选项数量以设计稿为准：四选一（一个正确 + 三个干扰）。`英语听力训练模块DESIGN.md` §25 写的是「中文三选一」，设计稿屏 52 写的是「三个错误选项」，两份文档冲突时取更新的那份；四个选项也能容纳更多误听类型，区分度更好（2026-08-31，用户确认）。
- 对齐设计稿「实现规格」：主操作 56dp、固定在底部不随内容滚动；对错同时给图标和文字，不单靠红绿，正确用自定义语义色 `correct`；进度条带「第 n 题，共 N 题」的 contentDescription；播放键带 stateDescription。
- 未对齐且已知：设计稿要求学习流内 TopAppBar 左侧用 close 图标，现有单词/语法/阅读/朗读/情景五个页面用的都是 ArrowBack，只改听力反而不一致，留作全局统一时一起改。AI 生成的加载态设计稿要求「步骤清单而非纯转圈」，阅读页也还是转圈，同样留作统一处理。
- 相关取舍：
  - 第三级提示（挖空英文）由本地按 `keyExpression` 从原句生成，不问 AI——挖空位置必须和重点表达严格一致，交给 AI 会对不上。因此 `keyExpression.en` 必须原样出现在句子里，本地校验强制这一条。
  - 答错固定记 20 分，对应 §21 的「完整字幕以后才理解：20」；不记 0 分是因为用户确实在揭晓页读到了英文。
  - 影视和游戏场景走 §15 的 Inspired Scene：写风格相同的原创台词，不照搬真实作品原话，授权说不清的东西不批量进库。

## D-021：生成边写边用，不再整批干等

- 状态：已确认（2026-09-01，用户要求"优化整体生成速度"）
- 决定：长生成一律"先给能用的那部分"，而不是等整批闭合：
  - 听力一轮十句改成**逐条交付**。流里每闭合一个句子对象就解析、校验、送到界面，攒够 3 句直接开练，
    剩下的边听边补；用户答得比写得快时停在"下一句还在写"，而不是把这一轮当成做完了。
  - 单词学习在**复习到期卡片期间**就把新词先写好（到期卡片 ≥ 3 张才提前发），点"学新词"时内容已在手上。
    语法讲解 → 练习题此前已经是这个做法，这次把单词对齐过来。
- 原因：这些等待都发生在用户已经决定要学的时候，等待时间越长越容易在这一步流失；而内容本身是顺序可用的，
  没有理由攒够一整批才让人开始。
- 代价与边界：提前生成有可能白花一次调用（复习几张卡就退出）。所以只在"人已经明显投入了"的时机提前发，
  并且只发一次、结果缓存；点进去时正在跑就等它，不重复发。
- 明确没做：TTS 音频预取。合成器是单实例，边播边预取会互相打断，风险大于收益，留待需要时再单独设计。

## D-022：模型按功能分别指定

- 状态：已确认（2026-09-01，用户要求）
- 决定：每个 AI 调用带一个 `AiTask`（听力/阅读/单词/语法/翻译/情景/点词与提问/测评/发音提示），
  provider 按它取模型。设置页「各功能使用的模型」逐项指定，模型清单用 `GET /models` 拉回来供选择；
  没指定过的功能跟随默认模型，默认模型一改它们自动跟着改。
- 原因：听力那种一次写十句带干扰项的调用和"点个词马上要出字"的调用对模型的要求正好相反，
  一个全局模型必然在某一头将就。
- 取舍：偏好里只存显式设过的覆盖项，不给九项各存一份默认值——否则改默认模型要挨个改，
  还会把"跟随"和"恰好一样"两种状态混成一种。
- 界面是两级**独立页面**（`settings/models` → `settings/models/{task}`），不是弹窗：要翻的是
  几十上百行的模型清单，还要在九个功能之间来回比对，浮窗放不下也滚不顺（2026-09-01，用户要求）。
- 默认模型本身也在这个页面改（列表第一行），不再只能在 onboarding 里定。
- 清单按 id 关键词筛掉不能对话的模型（生图/嵌入/语音/重排/审核）。只是按名字猜，所以留
  「显示全部」的口子，全被筛光时原样列出——宁可多列一个，也不能把新模型藏起来。

## D-023：等待期间必须区分"没接通"和"模型在想"

- 状态：已确认（2026-09-01，用户反馈"听力的接通中要很久"）
- 决定：流式调用把进度分成 `Connecting` / `Thinking` / `Writing` 三段。响应头一到就离开
  "接通中"；推理增量单独收，服务商肯给文本就显示尾巴，不给就说一句"推理模型会先想一会儿"。
- 原因：默认模型是推理模型，开口前的思考期没有正文增量，界面一动不动，全被误报成"接通中"——
  用户会以为是网络问题去查网络，其实只需要等。这两件事该做的反应不一样，就不能混成一个字样。
- 顺带修掉：增量块的 `content` 为 `null` 时整块解析失败（不少服务商第一块就是这样），
  以及推理长度不计入输出硬上限的漏洞。
- 还没做：把 `reasoning_effort` 之类的参数发出去真正减少思考时间，需要先确认服务商认不认。

## D-024：先量再修——「接通中」到底卡在哪一段

- 状态：已确认（2026-09-01，用户反馈改完文案后「接通中」仍然最久）
- 背景：「接通中」这一段跨了 DNS、建连、TLS、上传请求、等服务端返回响应头五件事，
  光知道"一共很久"没法修，只能猜。所以先把它拆开量出来，再动手。
- 决定：
  - OkHttp 装一个 `HttpTimingListener`，响应头到达（或失败）时打一行分段耗时到 `LazyDogAI`；
    请求上带 op tag，日志能对上是哪个调用。复用连接时没有 DNS/建连两段，正好做对照。
  - 收到第一个增量时打一行「首个思考 / 首个正文」，把服务端的思考时间也量出来。
  - DNS 结果 IPv4 优先。IPv6 不通但有 AAAA 记录的网络上（国内移动网络常见），
    OkHttp 会先在 IPv6 上干等满 connectTimeout（10 秒）才轮到 IPv4，这十秒全在「接通中」里。
    OkHttp 4.12 还没有 `fastFallback`，所以自己排序；不丢 IPv6，IPv6-only 网络上 IPv4 会立刻
    unreachable，不耽误。
  - 「换用 `max_completion_tokens`」的结论存进偏好，按模型记住。之前每一次调用都要先用
    `max_tokens` 撞一个 400 再换名重发，白搭一整个往返，且每次都付。
  - 等待页显示已等秒数：干等时它是唯一在动的东西，也让"到底多久"变成一个能说出口的数字。
- 明确没做：把结论缓存在进程内的静态集合里。偏好里已经有一份，再留一份全局可变状态，
  两处口径迟早不一致（测试之间互相污染就是第一个信号）。
